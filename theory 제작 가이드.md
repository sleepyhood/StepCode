## 목적

이 가이드는 `practice/data/theory/` 아래 이론 문서를 일관된 품질로 제작하기 위한 공통 규칙을 정의한다.
목표는 "읽고 이해되는 이론"과 "문제 풀이로 바로 연결되는 이론"이다.

---

## 1. 기본 원칙

1. 이론은 정의 암기보다 **실행 흐름 이해**를 우선한다.
2. 설명은 짧고 명확하게 쓰고, 예제로 바로 검증한다.
3. Python/C/Java는 **동일 개념·동일 의도**로 제시하고 문법만 바꾼다.
4. 이론의 각 섹션은 최소 1개 이상 문제 유형(`trace`, `short`, `code`)과 연결되도록 구성한다.

---

## 2. 문서 표준 구조

모든 `theory_lvXX_*.md`는 아래 순서를 기본으로 한다.

1. 개념 소개: 해당 문법/개념이 하는 일
2. 실행 흐름: 단계(순서) 중심 설명
3. 대표 패턴: 가장 자주 쓰는 코드 패턴 2~4개
4. 누적/분기 포인트: 상태 변화가 발생하는 지점 설명
5. 자주 틀리는 포인트: 경계값, 종료 직후 값, 입력 위치 등
6. 언어별 실전 팁: Python/C/Java 차이만 간단히 정리
7. 미니 체크: 2~4문항 즉시 확인 문제
8. 다음 학습: 연결되는 문제 세트/다음 단원 안내

---

## 3. 코드 예제 규칙

1. 예제는 짧게 유지한다. (핵심 문법 3~8줄 권장)
2. 한 섹션에서 한 포인트만 검증한다.
3. 변수명은 학습 친화적으로 통일한다. (`i`, `n`, `sumv`, `cnt`)
4. 누적 변수 초기화는 반드시 반복문 밖에 둔다.
5. 초급 단원에서는 축약 문법보다 정식 문법을 우선한다.

---

## 4. 입출력/추적 표기 규칙

1. 예제 입출력은 `io` 블록으로 통일한다.
2. 반복/누적 설명에는 `tracegrid`를 적극 사용한다.
3. `tracegrid` 시점은 반드시 명시한다.
   - 권장: "각 반복 직후"
4. `short` 유형(출력 예측)과 충돌하는 위치에는 정답 노출형 예시를 두지 않는다.

---

## 5. 이미지(SVG) 규칙

1. 이론 이미지 경로는 `practice/data/theory/images/`를 사용한다.
2. Markdown 삽입은 상대경로를 사용한다.
3. SVG는 본문 폭을 넘지 않도록 반응형으로 작성한다.
   - 권장 헤더: `<svg width="100%" viewBox="...">`
   - 권장 옵션: `preserveAspectRatio="xMinYMin meet"`
4. 고정 `width`/`height`만 사용하는 SVG는 지양한다.

---

## 6. 언어 병기 규칙

1. 같은 패턴은 Python/C/Java 순서로 제시한다.
2. 언어별 예제는 의미를 동일하게 유지한다.
3. 허용 차이:
   - Python: `range`, `input`, `print`
   - C: `for (...)`, `scanf`, `printf`
   - Java: `for (...)`, `Scanner` 또는 표준 입력 방식, `System.out.println`
4. 비허용 차이: 한 언어에만 추가 개념(배열, 고급 문법)을 넣는 구성

---

## 7. 문체/표현 규칙

1. 설명형 문체를 기본으로 한다. (`~입니다`, `~합니다`)
2. 지시형은 체크/연습 지점에서만 제한적으로 사용한다. (`~해보세요`)
3. 모호한 표현을 피한다.
   - 지양: "대충 반복된다"
   - 권장: "조건이 참일 때만 본문이 실행된다"
4. 용어는 문서 내에서 일관되게 유지한다. (`반복 변수`, `누적 변수`, `종료 직후 값`)

---

## 8. 품질 체크리스트

문서 완료 전 아래를 반드시 점검한다.

1. 표준 구조 8개 섹션이 모두 채워졌는가
2. 대표 패턴에 Python/C/Java 예제가 모두 있는가
3. `tracegrid`가 최소 1개 이상 포함되어 있는가
4. 경계값 실수와 종료 직후 값을 명시했는가
5. 미니 체크 문항이 즉시 채점 가능한 형태인가
6. 이미지가 본문 너비를 벗어나지 않는가
7. 다음 문제 세트/다음 단원 연결이 포함되어 있는가

---

## 9. 파일/이름 규칙

1. 문서명: `theory_lvXX_topic.md`
2. 이미지명: `topic_purpose.svg`
   - 예: `for_flow.svg`, `for_range_boundary.svg`
3. 관련 문제와 id 축을 맞춘다.
   - 예: `lv07_for` 이론 <-> `py_lv07_for_*`, `c_lv07_for_*`

