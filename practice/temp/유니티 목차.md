## 초심자 친화 목차 (개선안)

### Part A. Unity 기본 UI/Inspector

* A1. Inspector 기본(Static/Tag/Prefab) : **2번**
* A2. Unity 창 구조(Hierarchy/Scene/Project/Inspector) : **9번**
* A3. IDE 설정(External Script Editor) : **18번**
* A4. Scene View 배치/툴(Local/Global, Transform Tool, Snapping 등) : **29번**
* A5. Inspector 변수 노출(접근제어 public/private, SerializeField) : **13번**

---

### Part B. 기본 출력/로그 + 연산자 기초

* B1. 콘솔 출력(Debug.Log) : **8번**
* B2. 연산자 기초(=, ==, !=, ++, +) : **3번**
* B3. 조건 연산(==, ||) : **12번**
* B4. 나머지 연산(%) : **27번**
* B5. Unity/C# 명명 규칙(대소문자, 포함) : **17번 21번**

---

### Part C. 함수/접근제어/기초 문법

* C1. 함수 개념(T/F: void, return, 매개변수) : **25번**
* C2. static 문법(정적 메서드/필드 연관) : **28번**

---

### Part D. Null/예외/컬렉션 기초

* D1. NullReferenceException 원인(참조 기본값 null) : **1번**
* D2. null 비교 가능한 참조 GameObject 비교 : **10번**
* D3. 타입 불일치 비교로 인한 컴파일 오류(컬렉션 vs 기본형) : **37번**
* D4. Dictionary 초기화(리스트 기반 key=name) : **39번**

---

### Part E. Transform/Hierarchy/라이프사이클

* E1. Transform 멤버 오류 해결(중첩 클래스 배열 선언) : **4번**
* E2. 자식 Transform 배열 순회(GetChild/childCount) : **14번**
* E3. Awake/OnEnable 구분 + Parent/위치/회전 설정 : **26번**

---

### Part F. 입력(Input)

* F1. 입력 API(GetKey/Down/Up) : **38번**
* F2. 축 입력 + Transform 이동(Translate, GetAxis, deltaTime) : **22번**

---

### Part G. 오브젝트 생성/물리

* G1. 발사 입력 + Instantiate + 초기 속도(Rigidbody.velocity, TransformDirection) : **36번**
* G2. Rigidbody 참조 방식(GetComponent<Rigidbody>, MovePosition) : **11번**
* G3. AddForce로 발사 이동(transform.forward * Inspector 값) : **24번**
* G4. 투사체 재사용 초기화(Init) + Trigger 충돌(OnTriggerEnter) : **23번**

---

### Part H. UI

* H1. UI Text 점수 표시(Text, .text, ToString, 네임스페이스) : **6번**
* H2. 문자열 인수 받아 Text 갱신하는 메서드 선언 : **7번**
* H3. 클릭으로 오브젝트 토글(OnMouseUp, SetActive) : **16번**
* H4. Button.onClick 등록 위치(Start/Update/Trigger) 판단 : **30번**

---

### Part I. Animator

* I1. 점프 상태 구성(애니메이션 배치: Apex/Fall/Land) : **15번**
* I2. 상태머신 전환 개념(T/F: Entry/Default/Sub-State 등) : **31번**
* I3. 파라미터 타입별 Set 함수(SetInt/Float/Bool/Trigger) : **32번**
* I4. SetBool 사용(Attacking false) : **33번**
* I5. Play 모드 파라미터 조작(Trigger/Bools reset) : **35번**

---

### Part J. Material/Color

* J1. Color 출력(Debug.Log + color) : **20번**
* J2. Material.SetColor("_Color", Color.red) : **40번**

---

### Part K. DOTS/ECS 심화

* K1. ECS(Entities) 사용 여부 판단 : **5번**
* K2. ECS 사용 여부 판단 2(실제 ECS 구성 요소 확인) : **34번**
