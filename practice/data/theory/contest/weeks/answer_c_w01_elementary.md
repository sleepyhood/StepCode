# C Contest Week 01 Elementary Answer Sheet

| 문항 | 정답 | 연계 개념 | 해설 메모 |
| --- | --- | --- | --- |
| 1번 | ③ | 개념 1) 컴파일러/인터프리터/헤더 기능 판별 | 용어를 `번역 도구/실행 방식`으로 먼저 분류하면 컴파일러 선지만 남는다. |
| 2번 | ③ | 개념 2) `limits.h`와 `SHRT_MAX` 해석 | 상수 접두어 `SHRT_`를 기준으로 자료형을 연결하면 `short` 최댓값을 판별할 수 있다. |
| 3번 | ① | 개념 3) `short` 오버플로우와 정수 승격 | 연산은 `int`로 수행되지만 저장은 `short`로 돌아간다는 순서를 적용해 경계 순환을 확인한다. |
| 4번 | ② | 개념 1) 컴파일러/인터프리터/헤더 기능 판별, 개념 2) `limits.h`와 `SHRT_MAX` 해석 | 상수 이름 확인 후 해당 상수군을 제공하는 헤더를 대조하면 된다. |
| 5번 | ④ | 개념 1) 컴파일러/인터프리터/헤더 기능 판별 | 헤더-기능 표에서 `stdarg.h`의 역할을 먼저 고정하면 오답 연결을 제거할 수 있다. |
