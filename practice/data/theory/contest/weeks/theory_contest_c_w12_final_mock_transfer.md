# C 경시대회 W12 - 최종 모의/유형 전이

## 메타
- week: W12
- slug: final_mock_transfer
- audience: common, elementary, middle, high

## 학습 목표
- 누적/분기/탐색이 결합된 코드를 조건 단위로 분해해 판단하는 것이 목표입니다.
- 추적표 검산으로 중간값을 통제해 계산 실수를 줄이는 것이 목표입니다.
- 제한 시간 안에서 선풀이 문항과 보류 문항을 구분해 점수를 안정화하는 것이 목표입니다.

## 공통 이론 (COMMON)
<!-- audience:common -->
### 개념 1) 복합 조건 분해
- 개념 정의: 반복문 안에 조건문이 여러 개 있을 때, 어떤 조건이 상태를 바꾸는지 분리해 읽는 방법입니다.
- 판별 규칙: 조건문을 `누적 갱신`, `건너뛰기`, `종료`로 분류한다.
- 판별 규칙: 각 분기에서 변경되는 변수만 표시한다.
- 판별 규칙: 분기 순서를 바꾸지 않고 원래 흐름대로 판단한다.
- 추적 절차: 초기값을 적는다.
- 추적 절차: 반복 1회마다 조건 통과 여부를 체크한다.
- 추적 절차: 값이 바뀐 변수만 다음 줄에 반영한다.
- 추적 절차: 반복 종료 후 출력식에 최종 값을 대입해 검산한다.
- 오답 포인트: `if`와 `else`의 갱신 대상을 혼동하면 같은 입력에서도 누적값이 달라집니다.

| 분기 유형 | 대표 형태 | 상태 변화 |
| --- | --- | --- |
| 누적 갱신 | `if (cond) x += v` | `x`만 갱신 |
| 건너뛰기 | `if (cond) continue` | 이후 코드 미실행 |
| 종료 | `if (cond) break/return` | 반복/함수 즉시 종료 |
| 대체 분기 | `if ... else ...` | 한 회차에 한 경로만 실행 |

![복합 분기 조건을 분해해 누적 변수를 추적하는 흐름도](./data/theory/images/contest_w12_complex_branch_flow.svg)

예시 (기본):
```c
#include <stdio.h>

int main(void) {
    int v = 0;
    for (int i = 1; i <= 6; i++) {
        if (i % 2 == 0) v += i;
        else v -= 1;
    }
    printf("%d\n", v);
    return 0;
}
```
```io
input:
(없음)
output:
6
```

예시 (변형):
```c
#include <stdio.h>

int main(void) {
    int s = 0;
    for (int i = 3; i <= 12; i++) {
        if (i % 3 == 0) s += i;
        if (i % 5 == 0) s -= 2;
    }
    printf("%d\n", s);
    return 0;
}
```
```io
input:
(없음)
output:
13
```

예시 (함정):
```c
#include <stdio.h>

int main(void) {
    int x = 0;
    for (int i = 1; i <= 5; i++) {
        if (i < 3) x += 2;
        else if (i == 3) x += 10;
        else x -= 1;
    }
    printf("%d\n", x);
    return 0;
}
```
```io
input:
(없음)
output:
12
```

### 개념 2) 추적표 기반 검산
- 개념 정의: 실행 흐름을 표로 기록해 계산 과정과 출력값을 일치시키는 검산 방법입니다.
- 판별 규칙: 열은 `단계`, `조건 결과`, `핵심 변수` 3개로 최소화한다.
- 판별 규칙: 조건이 참/거짓인 이유를 식으로 적는다.
- 판별 규칙: 출력 직전 행을 반드시 별도 표시한다.
- 추적 절차: 초기 상태 행을 먼저 작성한다.
- 추적 절차: 반복 1회마다 변수 변화가 있는 행만 추가한다.
- 추적 절차: 마지막 행 값으로 출력을 계산하고, 루프 조건과 횟수를 다시 대조한다.
- 오답 포인트: 머릿속 계산으로 중간 행을 생략하면 반복 횟수와 경계값에서 누락이 발생합니다.

| 열 이름 | 기록 내용 | 실수 방지 포인트 |
| --- | --- | --- |
| 단계 | `i` 값/반복 회차 | 누락 회차 여부 확인 |
| 조건 결과 | 참/거짓 + 판정식 | 분기 경로 착오 방지 |
| 핵심 변수 | 갱신된 변수의 새 값 | 이전 값과 구분 기록 |
| 출력 직전 | 출력식 대입 결과 | 최종 검산용 별도 행 |

![추적표 열 구성과 출력 직전 검산 순서를 보여주는 도식](./data/theory/images/contest_w12_trace_table_check.svg)

예시 (기본):
```c
#include <stdio.h>

int main(void) {
    int t = 0;
    for (int i = 0; i < 4; i++) t += i * 2;
    printf("%d\n", t);
    return 0;
}
```
```io
input:
(없음)
output:
12
```

예시 (변형):
```c
#include <stdio.h>

int main(void) {
    int a = 1, b = 3;
    for (int i = 0; i < 3; i++) {
        int n = a + b;
        a = b;
        b = n;
    }
    printf("%d %d\n", a, b);
    return 0;
}
```
```io
input:
(없음)
output:
8 13
```

예시 (함정):
```c
#include <stdio.h>

int main(void) {
    int c = 0;
    for (int i = 1; i <= 5; i++) {
        if (i == 4) continue;
        c += i;
    }
    printf("%d\n", c);
    return 0;
}
```
```io
input:
(없음)
output:
11
```

### 개념 3) 시간 안배와 선택 전략
- 개념 정의: 제한 시간에서 풀이 순서를 조정해 확정 점수를 먼저 확보하는 운영 전략입니다.
- 판별 규칙: `즉시 풀이`, `검산 후 제출`, `보류`로 문항 상태를 분류한다.
- 판별 규칙: 추적표 6행 이내로 끝나면 즉시 풀이 대상으로 둔다.
- 판별 규칙: 이분 탐색/경계 검증이 필요한 문항은 검산 시간을 별도로 배정한다.
- 추적 절차: 시작 30초 안에 입력 구조와 핵심 변수를 표시한다.
- 추적 절차: 1차 풀이에서 확정 가능한 문항을 먼저 제출한다.
- 추적 절차: 남은 시간에 보류 문항을 경계값 중심으로 재검토한다.
- 오답 포인트: 어려운 문항 한 개에 시간을 고정하면 쉬운 문항 확정 기회를 놓치게 됩니다.

| 문항 상태 | 판단 기준 | 권장 행동 |
| --- | --- | --- |
| 즉시 풀이 | 추적표 6행 이내/규칙 명확 | 바로 풀이 후 제출 |
| 검산 후 제출 | 경계값/분기 다수 | 1회 풀이 + 1회 검산 |
| 보류 | 핵심 규칙 미확정 | 체크표시 후 다음 문항 이동 |
| 재진입 | 남은 시간 20% 이상 | 경계 입력부터 재검토 |

![시험 시간 내 문항 선택과 보류 전략 보드를 나타내는 도식](./data/theory/images/contest_w12_time_strategy_board.svg)

예시 (기본):
```text
총 20분, 5문항
- 1차 10분: 추적표 6행 이내 문항 3개 우선 해결
- 2차 7분: 이분 탐색/경계 검증 문항 1개 해결
- 3차 3분: 제출값 재확인 및 단위/부호 검산
```

예시 (변형):
```text
총 25분, 6문항
- 1차 12분: 조건 분해형 4문항 처리
- 2차 8분: 탐색형 1문항 처리
- 3차 5분: 보류 1문항 핵심 분기만 확인
```

예시 (함정):
```text
오답 패턴
- 시작부터 최난도 1문항에 12분 사용
- 중간 난도 2문항 미시도
- 마지막 3분에 추측 제출
```

### 공통 미니 체크 (필수 제출)
문항:
1. 복합 조건 분해에서 오늘 가장 자주 놓친 분기 1개를 적으세요.
2. 추적표 검산 시 반드시 적어야 하는 열 3개를 적으세요.
3. 다음 실전에서 본인이 적용할 시간 안배 규칙 1개를 적으세요.

답안 작성:
1. 정답: [ ] / 근거: [ ]
2. 정답: [ ] / 근거: [ ]
3. 정답: [ ] / 근거: [ ]

## 초등 트랙 (ELEMENTARY)
<!-- audience:elementary -->
### 초등 포인트
- 반복문 1회 단위로 조건 통과 여부를 분리해 적는 연습이 우선입니다.

### 초등 연계 실습
실습 목표:
- 조건 통과 여부를 기록해 누적값을 계산합니다.

실습 문제:
```c
#include <stdio.h>

int main(void) {
    int n = 7;
    int s = 0;
    for (int i = 1; i <= n; i++) {
        if (i % 3 == 0) s += i;
    }
    printf("%d\n", s);
    return 0;
}
```

체크포인트:
1. `i` 값별 조건 통과/미통과를 구분했는가?
2. 누적 변수 `s`의 초기값과 최종값을 모두 적었는가?

## 중등 트랙 (MIDDLE)
<!-- audience:middle -->
### 중등 포인트
- 누적 조건과 경계 조건을 같은 표에서 함께 검산하는 습관이 필요합니다.

### 중등 연계 실습
실습 목표:
- 경계값을 포함한 분기 흐름을 표로 추적합니다.

실습 문제:
```c
#include <stdio.h>

int main(void) {
    int a[6] = {5, 7, 2, 9, 4, 8};
    int c = 0, cnt = 0;
    for (int i = 0; i < 6; i++) {
        if (a[i] <= 6) cnt++;
        else c += a[i];
    }
    printf("%d %d\n", cnt, c);
    return 0;
}
```

체크포인트:
1. 경계값(`<= 6`) 처리 문항을 별도 표시했는가?
2. 분기별로 어떤 변수가 갱신되는지 분리해 적었는가?

## 고등 트랙 (HIGH)
<!-- audience:high -->
### 고등 포인트
- 탐색형 문항에서 `가능/불가능 판정`과 `탐색 구간 이동`을 분리해야 합니다.

### 고등 연계 실습
실습 목표:
- 판정 함수 관점으로 탐색 구간 이동 근거를 정리합니다.

실습 문제:
```c
#include <stdio.h>

int main(void) {
    int a[5] = {6, 8, 5, 7, 4};
    int l = 0, r = 30, ans = -1;

    while (l <= r) {
        int mid = (l + r) / 2;
        int cnt = 1, sum = 0;
        for (int i = 0; i < 5; i++) {
            if (sum + a[i] <= mid) sum += a[i];
            else {
                cnt++;
                sum = a[i];
            }
        }

        if (cnt <= 3) {
            ans = mid;
            r = mid - 1;
        } else {
            l = mid + 1;
        }
    }

    printf("%d\n", ans);
    return 0;
}
```

체크포인트:
1. `cnt <= 3` 판정이 참일 때 구간을 왜 왼쪽으로 이동하는지 설명했는가?
2. `l`, `r`, `mid`, `ans` 변화를 최소 3회 이상 기록했는가?
