# C Contest Week 03 High Problem Set

## 범위
- Week 03 core source pool mapping
- 분반: 고등
- W03 개념 1~3 적용

## 문제 1
연계 개념: 개념 1) 우선순위와 괄호 전략, 개념 3) 비트 연산 마스크
다음 코드의 실행 결과로 올바른 것을 고르시오.

```c
#include<stdio.h>
int main()
{
    int x = 12;
    int y = 15;
    int z = 18;
    printf("%d", (x*y)>(y&z)?(x+y):(y+z));
    return 0;
}
```
① 0  ② 27  ③ 30  ④ 33  ⑤ 180

## 문제 2
연계 개념: 개념 3) 비트 연산 마스크
다음 코드의 실행 결과로 올바른 것을 고르시오.

```c
#include <stdio.h>
int main()
{
    int a[5] = {빈 칸};
    int b = 0;
    for(int i=0 ; i<5 ; i++) {
        b ^= a[i];
    }
    printf("%d\n", b);
    return 0;
}
```
① 0, 1, 2, 3, 4  
② 1, 2, 3, 4, 4  
③ 1, 3, 1, 3, 1  
④ 0, 0, 0, 1, 1  
⑤ 10, -10, 10, -10, 10

## 문제 3
연계 개념: 개념 1) 우선순위와 괄호 전략
다음 코드의 실행 결과로 올바른 것을 고르시오.

```c
#include<stdio.h>
int main()
{
    int x = 12;
    int y = 15;
    printf("%d", x>y?(x+y):(x*y));
    return 0;
}
```

⓵ 3  
⓶ 12  
⓷ 15  
⓸ 27  
⓹ 180  

## 문제 4
연계 개념: 개념 1) 우선순위와 괄호 전략
다음 코드의 실행 결과로 올바른 것을 고르시오.

```c
#include<stdio.h>
int main()
{
    int a = (25 / 4) * 3;
    int b = (a * a) / a;
    printf("%d %d", a+b, a-b);
}
```

⓵ 36 0  
⓶ 35 1  
⓷ 35 0  
⓸ 34 1  
⓹ 37 0  

## 문제 5
연계 개념: 개념 2) 논리 연산 단락 평가
다음 조건식으로 빈 칸을 채울 때 올바른 것을 고르시오.

주어진 연도가 윤년이면 1, 아니면 0을 반환하는 함수를 만든다고 할 때 빈 칸에 들어갈 코드로 알맞은 것을 고르시오.

```c
int leapYear(int y) {
    if([빈 칸]) return 0;  // 빈 칸
    else return 1;
}
```

⓵ (y % 4 == 0 && y % 100 != 0) && y % 400 == 0  
⓶ (y % 4 == 0 && y % 100 != 0) || y % 400 == 0  
⓷ (y % 4 != 0 && y % 100 == 0) || y % 400 != 0  
⓸ (y % 4 != 0 || y % 100 == 0) || y % 400 != 0  
⓹ (y % 4 != 0 || y % 100 == 0) && y % 400 != 0  

