# Python Contest Week 02 Answer Sheet

## 작성 규칙
- 정답: 객관식은 보기 번호(예: ③), 주관식은 값/문자열/코드
- 해설 메모: 핵심 풀이 근거를 1~3줄로 작성

## PY_1회차

| 문항 | 정답 | 해설 메모 |
| --- | --- | --- |
| 9번 | ③ | `range(3, 35, 2)`는 3~33까지 총 16회 반복된다. 매회 `+2`이므로 `5 + 16*2 = 37`, 따라서 ③. |

## PY_2회차

| 문항 | 정답 | 해설 메모 |
| --- | --- | --- |
| 1번 | ④ | `range(100, 2024, 150)`은 13개 항(100~1900)이고 등차수열 합은 `13*(100+1900)/2 = 13000`. 따라서 ④. |
| 2번 | ④ | 홀수일 때 더하면 `1+3+5+7=16`, `i%3==1`일 때 빼면 `1+4+7=12`, 최종 `16-12=4`. 따라서 ④. |

## PY_3회차

| 문항 | 정답 | 해설 메모 |
| --- | --- | --- |
| 1번 | ③ | 바깥 반복 `range(1,30,2)`는 15회, 안쪽 반복 `range(2,20,3)`는 6회 실행된다. `a += 1` 총 `15*6=90`번이므로 ③. |
| 2번 | ④ | `PY_2회차 2번`과 동일 로직: 홀수 합 16, `i%3==1` 항 합 12를 빼서 결과는 4. 따라서 ④. |

