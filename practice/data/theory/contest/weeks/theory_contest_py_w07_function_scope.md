# Python 경시대회 W07 함수/스코프

## 메타
- week: W07
- slug: function_scope
- audience: common, elementary, middle, high

## 학습 목표
- 함수 정의와 전역 변수 선언 키워드를 정확히 구분하는 것이 목표입니다.
- 지역/전역 스코프 충돌 시 값이 어떻게 읽히고 갱신되는지 추적하는 것이 목표입니다.
- 재귀 함수에서 호출 횟수 누적 변수를 해석해 출력값을 예측하는 것이 목표입니다.
- 짝수/홀수 분기(`x & 1 == 0`)를 연산자 우선순위와 함께 설명할 수 있는 것이 목표입니다.

## 대상 난이도
- 공통: 초등, 중등, 고등
- 분기: ELEMENTARY, MIDDLE, HIGH

## 공통 이론 (COMMON)
<!-- audience:common -->
### 핵심 개념 요약
- 주제: 함수, 스코프, 전역/지역
- 운영: 개념 20분 + 예측형 25분 + 구현형 15분

### 개념 1) 함수 선언/전역 선언 키워드
- 개념 정의: 함수를 만들 때와 함수 내부에서 전역 변수를 수정할 때 사용하는 문법을 구분하는 유형입니다.
- 판별 규칙: 함수 정의는 `def`, 함수 내부 전역 수정 선언은 `global`이다.
- 추적 절차: 함수 헤더 확인 -> 내부에서 값 대입 여부 확인 -> 전역 선언 필요 여부 판단 순으로 진행합니다.
- 오답 포인트: `func`, `static`, `local` 같은 유사 단어를 문법 키워드로 착각하면 오답이 됩니다.

예시 (기본):
```python
a = 10

def f():
    global a
    a += 5

f()
print(a)
```
```io
input:
(없음)
output:
15
```

예시 (변형):
```python
def add(x, y):
    return x + y

print(add(3, 4))
```
```io
input:
(없음)
output:
7
```

예시 (함정):
```python
x = 1

def g():
    x = 2
    print(x)

g()
print(x)
```
```io
input:
(없음)
output:
2
1
```

### 개념 2) 지역/전역 스코프 충돌 추적
- 개념 정의: 같은 이름의 변수가 함수 안팎에 있을 때 실제 참조 대상을 판별하는 유형입니다.
- 판별 규칙: 함수 내부에서 대입이 발생하면 기본적으로 지역 변수로 취급한다.
- 추적 절차: 변수 이름 수집 -> 함수 내부 대입 유무 확인 -> 읽기/쓰기 스코프 분리 -> 출력 위치별 값 확정 순으로 추적합니다.
- 오답 포인트: 대입이 있는데 `global` 선언이 없으면 외부 값이 바뀐다고 오해하기 쉽습니다.

![전역/지역 스코프 메모리 박스 다이어그램](./data/theory/images/contest_w07_scope_memory_boxes.svg)

예시 (기본):
```python
a = 3

def f():
    a = 9
    print(a)

f()
print(a)
```
```io
input:
(없음)
output:
9
3
```

예시 (변형):
```python
a = 3

def f():
    global a
    a = 9

f()
print(a)
```
```io
input:
(없음)
output:
9
```

예시 (함정):
```python
a = 5

def f():
    try:
        a += 1
    except UnboundLocalError:
        print("UnboundLocalError")

f()
```
```io
input:
(없음)
output:
UnboundLocalError
```

### 개념 3) 재귀 호출 횟수 누적(`a += 1`)
- 개념 정의: 재귀 함수 진입 시마다 카운터를 증가시키고 종료 조건까지 몇 번 호출되는지 계산하는 유형입니다.
- 판별 규칙: `a += 1`이 함수 첫 부분에 있으면 현재 호출도 횟수에 포함된다.
- 추적 절차: 시작값 기록 -> 매 단계에서 `a` 증가 -> 다음 인자 계산 -> 종료 조건 `x == 1` 도달 시 누적 횟수 확정 순으로 풀이합니다.
- 오답 포인트: 종료 호출(마지막 `x==1`)을 카운트에서 빼면 1 작게 계산됩니다.

![재귀 호출 스택과 호출 횟수 카운트](./data/theory/images/contest_w07_recursive_call_stack_counter.svg)

예시 (기본):
```python
a = 0

def f(x):
    global a
    a += 1
    if x == 1:
        return
    f(x - 1)

f(4)
print(a)
```
```io
input:
(없음)
output:
4
```

예시 (변형):
```python
a = 0

def f(x):
    global a
    a += 1
    if x == 1:
        return
    if x % 2 == 0:
        f(x // 2)
    else:
        f(x * 3 + 1)

f(3)
print(a)
```
```io
input:
(없음)
output:
8
```

예시 (함정):
```python
a = 0

def f(x):
    global a
    if x == 1:
        return
    a += 1
    f(x - 1)

f(4)
print(a)
```
```io
input:
(없음)
output:
3
```

### 개념 4) 짝수/홀수 분기와 `x & 1 == 0`
- 개념 정의: 비트 연산으로 홀짝을 판정해 재귀 분기 경로를 선택하는 유형입니다.
- 판별 규칙: `x & 1 == 0`은 `(x & 1) == 0`으로 해석하며, 참이면 짝수다.
- 추적 절차: 현재 `x`의 최하위 비트 확인 -> 짝수/홀수 분기 선택 -> 다음 `x` 생성 -> 호출 경로 표 작성 순으로 진행합니다.
- 오답 포인트: 비교 우선순위를 잘못 이해하면 분기식을 다른 의미로 오해할 수 있습니다.

예시 (기본):
```python
for x in [4, 7, 10]:
    if x & 1 == 0:
        print("E")
    else:
        print("O")
```
```io
input:
(없음)
output:
E
O
E
```

예시 (변형):
```python
for x in [2, 3]:
    print((x & 1) == 0)
```
```io
input:
(없음)
output:
True
False
```

예시 (함정):
```python
x = 3
print(x & (1 == 0))
```
```io
input:
(없음)
output:
0
```

### 개념 5) 호출 경로 길이 비교 전략
- 개념 정의: 여러 시작값 중 재귀 경로가 가장 긴 후보를 선택하는 유형입니다.
- 판별 규칙: 동일 함수/규칙에서는 각 후보의 `a` 값을 독립적으로 측정해 최대값을 비교한다.
- 추적 절차: 후보별 초기화 -> 함수 실행 -> 호출 횟수 기록 -> 최댓값 비교 -> 대응 보기 번호 선택 순으로 처리합니다.
- 오답 포인트: 후보를 연속 실행하면서 `a`를 초기화하지 않으면 비교가 무효가 됩니다.

예시 (기본):
```python
def steps(n):
    c = 0
    while n != 1:
        c += 1
        if n % 2 == 0:
            n //= 2
        else:
            n = 3 * n + 1
    return c + 1

cands = [3, 5, 7]
best = max(cands, key=steps)
print(best)
```
```io
input:
(없음)
output:
7
```

예시 (변형):
```python
def chain_len(n):
    cnt = 1
    while n != 1:
        cnt += 1
        if n % 2 == 0:
            n //= 2
        else:
            n = 3 * n + 1
    return cnt

print(chain_len(9))
print(chain_len(11))
```
```io
input:
(없음)
output:
20
15
```

예시 (함정):
```python
a = 0

def f(x):
    global a
    a += 1
    if x == 1:
        return
    f(x - 1)

for n in [3, 4]:
    f(n)
print(a)
```
```io
input:
(없음)
output:
7
```

### 공통 미니 체크
1. 함수 정의 키워드와 전역 선언 키워드를 각각 쓰시오.
2. 함수 안에서 대입이 있는 변수는 기본적으로 어느 스코프에 속하는지 쓰시오.
3. 재귀 호출 횟수를 셀 때 종료 호출 포함 여부를 쓰시오.
4. `x & 1 == 0`의 해석 형태를 괄호로 명시하시오.

## 초등 트랙 (ELEMENTARY)
<!-- audience:elementary -->
### 초등 포인트
- 지역변수 중심 추적

### 초등 이론 보강
- `def`와 `global`을 문장 완성 문제로 반복해 익힙니다.
- 함수 호출 전/후 전역 변수 값을 표로 나눠 적는 연습을 합니다.

초등 규칙 카드:
1. `def`는 함수 시작이다.
2. 전역 값 수정 전에는 `global`을 확인한다.
3. 출력 줄 위치마다 값을 따로 판단한다.

### 초등 연계 연습
1. 전역 변수 1개를 함수에서 증가시키는 코드를 작성하시오.
2. 같은 이름 지역 변수를 선언했을 때 바깥 값이 유지되는지 확인하시오.

### 초등 오답 패턴
- 함수 내부 값이 바깥 값도 자동 변경한다고 가정해 오답이 발생합니다.

## 중등 트랙 (MIDDLE)
<!-- audience:middle -->
### 중등 포인트
- 전역/지역 충돌 케이스

### 중등 이론 보강
- `global` 유무에 따른 실행 결과를 나란히 비교합니다.
- 재귀에서 분기 조건이 바뀔 때 호출 횟수가 어떻게 달라지는지 단계표로 정리합니다.

중등 판별 루틴:
1. 스코프 선언(`global`) 여부를 먼저 본다.
2. 종료 조건과 분기식을 확인한다.
3. 호출 횟수 누적 위치를 점검한다.

### 중등 연계 연습
1. 같은 함수에서 `a += 1` 위치를 앞/뒤로 옮겨 결과를 비교하시오.
2. 후보값 3개의 재귀 호출 횟수를 계산해 최대 후보를 고르시오.

### 중등 오답 패턴
- 종료 조건 도달 호출을 누락해 카운트를 1 적게 계산합니다.

## 고등 트랙 (HIGH)
<!-- audience:high -->
### 고등 포인트
- 재귀 분기 최적 비교

### 고등 이론 보강
- 다수 후보의 호출 길이 비교를 자동화하는 함수를 설계합니다.
- 연산자 우선순위와 스코프 규칙을 함께 점검해 추론 오류를 줄입니다.

고등 판별 프레임:
1. 스코프 규칙을 먼저 고정한다.
2. 분기식 해석을 괄호로 명시한다.
3. 후보별 호출 길이를 독립 측정해 최대를 선택한다.

### 고등 연계 연습
1. 후보 집합이 주어졌을 때 호출 횟수 최댓값을 반환하는 함수를 작성하시오.
2. 동일 문제를 재귀와 반복(`while`) 두 방식으로 구현해 결과를 대조하시오.

### 고등 오답 패턴
- 연산자 우선순위를 검증하지 않고 분기식을 임의 해석합니다.

## 적용 유형
- 함수/키워드 문법 판별
- 전역/지역 스코프 결과 예측
- 재귀 호출 횟수 계산
- 홀짝 분기 기반 재귀 경로 추적
- 후보 비교형 최댓값 선택

## {view:teacher} 과제
- 초등: 6문항
- 중등: 8문항
- 고등: 10~12문항

## {view:teacher} 평가
- 항목: 주간퀴즈 3
- 오답코드: 스코프, 종료조건, 분기오해, 누적위치, 우선순위

평가 체크:
1. `def/global` 키워드를 구분했는가
2. 지역/전역 변수 충돌을 출력 위치별로 설명했는가
3. 재귀 호출 횟수에서 종료 호출 포함 여부를 반영했는가
4. `x & 1 == 0` 해석을 괄호로 명확히 제시했는가

## {view:teacher} 교사 메모
- 웹 렌더 규칙: COMMON + 선택 학년 섹션만 노출합니다.
- 수업 후 오답코드를 기준으로 다음 주차 보강 포인트를 기록합니다.
