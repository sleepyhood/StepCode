---
id: "c_lv07_for_c01"
title: "C for문 챌린지 1회차"
categoryId: "c_for"
availableLanguages: ["c"]
---

# C for문 챌린지 1회차

### Q1. Trace 1. n개 입력 중 짝수만 더하기

입력이 `3, 8, 4, 1, 6`일 때, 각 반복 직후의 i, num, sum을 표에 채우세요.

```c
int n = 5;
int sum = 0;
for (int i = 1; i <= n; i++) {
    int num;
    scanf("%d", &num);
    if (num % 2 == 0) {
        sum += num;
    }
}
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **Trace 정답 표:** [["1", "3", "0"], ["2", "8", "8"], ["3", "4", "12"], ["4", "1", "12"], ["5", "6", "18"]]
<!-- ANSWER_END -->

### Q2. Trace 2. n개 입력 중 홀수들의 평균

입력이 `9, 4, 7, 3, 8`일 때, 홀수만 이용해 평균을 구합니다. 각 반복 직후의 i, num, sum, count를 표에 채우세요.

```c
int n = 5;
int sum = 0;
int count = 0;
for (int i = 1; i <= n; i++) {
    int num;
    scanf("%d", &num);
    if (num % 2 != 0) {
        sum += num;
        count++;
    }
}
int avg = sum / count;
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **Trace 정답 표:** [["1", "9", "9", "1"], ["2", "4", "9", "1"], ["3", "7", "16", "2"], ["4", "3", "19", "3"], ["5", "8", "19", "3"]]
<!-- ANSWER_END -->

### Q3. Reverse 1. 1부터 n까지의 평균 역추론

아래 코드의 출력(avg)이 `5`였습니다. 단, `1 <= n <= 10`일 때 가능한 n 중 가장 큰 값을 쓰세요.

```c
int sum = 0;
for (int i = 1; i <= n; i++) {
    sum += i;
}
int avg = sum / n;
printf("%d\n", avg);
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답:** `10`
<!-- ANSWER_END -->

### Q4. Short 1. 최댓값과 종료 직후 값

입력이 `8, 3, 12, 7, 10`일 때, 출력되는 `i max`를 공백으로 구분해 쓰세요.

```c
int max;
int num;
scanf("%d", &max);
int i;
for (i = 2; i <= 5; i++) {
    scanf("%d", &num);
    if (num > max) {
        max = num;
    }
}
printf("%d %d\n", i, max);
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답:** `6 12`
<!-- ANSWER_END -->

### Q5. Code 1. 짝수 판별 조건식

입력값 num이 짝수일 때만 sum에 더하려고 합니다. `if`의 조건식을 한 줄로 작성하세요.

```c
if (/* TODO */) {
    sum += num;
}
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
num % 2 == 0
```
<!-- ANSWER_END -->

### Q6. Code 2. 최댓값 갱신 조건식

새 입력값 num이 현재 최댓값 max보다 클 때만 갱신하려고 합니다. `if`의 조건식을 한 줄로 작성하세요.

```c
if (/* TODO */) {
    max = num;
}
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
num > max
```
<!-- ANSWER_END -->
