---
id: "java_array_lv10_c02"
title: "Java 배열 Lv10 챌린지 2회차"
categoryId: "java_array"
availableLanguages: ["java"]
---

# Java 배열 Lv10 챌린지 2회차

### Q1. MCQ 1. 빈도 배열 크기

0부터 9까지의 숫자가 입력으로 들어올 때, 각 숫자의 등장 횟수를 세기 위한 빈도 배열 cnt의 올바른 크기를 고르세요.

- **A**: int[] cnt = new int[9];
- **B**: int[] cnt = new int[10];
- **C**: int[] cnt = new int[11];
- **D**: int[] cnt = new int[0];

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답 인덱스:** 1
<!-- ANSWER_END -->

### Q2. Code 1. 빈도 증가

아래 코드에서 주석 위치에 들어갈 **한 줄**을 작성하세요.
입력 x(0~9)의 등장 횟수를 cnt에 누적해야 합니다.

```c
int[] cnt = new int[10];
for (int i = 0; i < n; i++) {
    int x = sc.nextInt();
    // TODO: x의 빈도를 1 증가시키는 한 줄을 작성하시오.
}
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
cnt[x]++;
```
<!-- ANSWER_END -->

### Q3. Short 1. 빈도 실행 결과

아래 코드가 실행된 뒤, cnt[0]의 값은 무엇인가요?

```c
int[] cnt = new int[10];
int[] data = {0, 2, 0, 9, 0};
for (int i = 0; i < data.length; i++) {
    int x = data[i];
    cnt[x]++;
}
// 여기서 cnt[0]의 값은?
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답:** `3`
<!-- ANSWER_END -->

### Q4. MCQ 2. 최빈값 tie-break

다음 중 최빈값을 찾을 때, 빈도가 같으면 **더 작은 숫자**를 선택하는 조건식으로 올바른 것을 고르세요.
(bestCnt, bestNum, cnt[i]가 이미 정의되어 있다고 가정합니다.)

- **A**: cnt[i] > bestCnt && i < bestNum
- **B**: cnt[i] >= bestCnt && i < bestNum
- **C**: cnt[i] > bestCnt || (cnt[i] == bestCnt && i < bestNum)
- **D**: cnt[i] == bestCnt || i < bestNum

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답 인덱스:** 2
<!-- ANSWER_END -->

### Q5. Code 2. 최빈값 갱신(2줄)

아래 코드에서 주석 위치에 들어갈 내용을 작성하세요.
조건을 만족하면 bestCnt와 bestNum을 갱신해야 합니다.
(주석 위치에 **두 줄**로 작성하세요.)

```c
int bestNum = 0;
int bestCnt = -1;
for (int i = 0; i < 10; i++) {
    if (cnt[i] > bestCnt || (cnt[i] == bestCnt && i < bestNum)) {
        // TODO: bestCnt와 bestNum을 갱신하는 두 줄을 작성하시오.
    }
}
System.out.println(bestNum);
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
bestCnt = cnt[i];
bestNum = i;
```
<!-- ANSWER_END -->

### Q6. MCQ 3. 누적합(prefix) 점화식

prefix[0] = 0이고, prefix[i+1]에 arr[0]부터 arr[i]까지의 합을 저장하려고 합니다.
다음 중 올바른 한 줄을 고르세요. (i는 0부터 시작)

- **A**: prefix[i] = prefix[i] + arr[i];
- **B**: prefix[i + 1] = prefix[i] + arr[i];
- **C**: prefix[i] = prefix[i + 1] + arr[i];
- **D**: prefix[i + 1] = prefix[i] + arr[i + 1];

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답 인덱스:** 1
<!-- ANSWER_END -->

### Q7. Code 3. prefix 채우기

아래 코드에서 주석 위치에 들어갈 **한 줄**을 작성하세요.
(prefix 배열은 길이가 n+1이고 prefix[0]=0입니다.)

```c
int[] prefix = new int[n + 1];
prefix[0] = 0;
for (int i = 0; i < n; i++) {
    // TODO: prefix[i+1]을 채우는 한 줄을 작성하시오.
}
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
prefix[i + 1] = prefix[i] + arr[i];
```
<!-- ANSWER_END -->

### Q8. Short 2. prefix로 구간합

prefix 배열이 아래와 같을 때, l=1, r=2 (0-index) 구간의 합은 얼마인가요?
(구간합 공식: sum = prefix[r+1] - prefix[l])

```c
int[] prefix = {0, 2, 5, 9, 10};
int l = 1;
int r = 2;
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답:** `7`
<!-- ANSWER_END -->

### Q9. Code 4. prefix로 구간합 한 줄

아래 코드에서 주석 위치에 들어갈 **한 줄**을 작성하세요.
(0-index, l~r 구간합을 계산해야 합니다.)

```c
int sum;
// TODO: prefix를 이용해 l~r 구간합을 sum에 저장하는 한 줄을 작성하시오.
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
sum = prefix[r + 1] - prefix[l];
```
<!-- ANSWER_END -->
