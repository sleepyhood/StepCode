---
id: "c_lv10_array_c02"
title: "C 배열 Lv10 챌린지 2회차"
categoryId: "c_array"
availableLanguages: ["c"]
---

# C 배열 Lv10 챌린지 2회차

### Q1. MCQ 1. 빈도 배열 크기

0부터 9까지의 숫자가 입력으로 들어올 때, 각 숫자의 등장 횟수를 세기 위한 빈도 배열 cnt의 올바른 크기를 고르세요.

- **A**: int cnt[9] = {0};
- **B**: int cnt[10] = {0};
- **C**: int cnt[11] = {0};
- **D**: int cnt[] = {0};

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답 인덱스:** 1
<!-- ANSWER_END -->

### Q2. Code 1. 빈도 증가

아래 코드에서 주석 위치에 들어갈 **한 줄**을 작성하세요.
입력 x(0~9)의 등장 횟수를 cnt에 누적해야 합니다.

```c
int cnt[10] = {0};
for (int i = 0; i < n; i++) {
    int x;
    scanf("%d", &x);
    // TODO: x의 빈도를 1 증가시키는 한 줄을 작성하시오.
}
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
cnt[x]++;
```
<!-- ANSWER_END -->

### Q3. Short 1. 빈도 실행 결과

아래 코드가 실행된 뒤, cnt[0]의 값은 무엇인가요?

```c
int cnt[10] = {0};
int data[5] = {0, 2, 0, 9, 0};
for (int i = 0; i < 5; i++) {
    int x = data[i];
    cnt[x]++;
}
// 여기서 cnt[0]의 값은?
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답:** `3`
<!-- ANSWER_END -->

### Q4. MCQ 2. 최빈값 tie-break

다음 중 최빈값을 찾을 때, 빈도가 같으면 **더 작은 숫자**를 선택하는 조건식으로 올바른 것을 고르세요.
(best_cnt, best_num, cnt[i]가 이미 정의되어 있다고 가정합니다.)

- **A**: cnt[i] > best_cnt && i < best_num
- **B**: cnt[i] >= best_cnt && i < best_num
- **C**: cnt[i] > best_cnt || (cnt[i] == best_cnt && i < best_num)
- **D**: cnt[i] == best_cnt || i < best_num

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답 인덱스:** 2
<!-- ANSWER_END -->

### Q5. Code 2. 최빈값 갱신(2줄)

아래 코드에서 주석 위치에 들어갈 내용을 작성하세요.
조건을 만족하면 best_cnt와 best_num을 갱신해야 합니다.
(주석 위치에 **두 줄**로 작성하세요.)

```c
int best_num = 0;
int best_cnt = -1;
for (int i = 0; i < 10; i++) {
    if (cnt[i] > best_cnt || (cnt[i] == best_cnt && i < best_num)) {
        // TODO: best_cnt와 best_num을 갱신하는 두 줄을 작성하시오.
    }
}
printf("%d\n", best_num);
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
best_cnt = cnt[i];
best_num = i;
```
<!-- ANSWER_END -->

### Q6. MCQ 3. 누적합(prefix) 점화식

prefix[0] = 0이고, prefix[i+1]에 arr[0]부터 arr[i]까지의 합을 저장하려고 합니다.
다음 중 올바른 한 줄을 고르세요. (i는 0부터 시작)

- **A**: prefix[i] = prefix[i] + arr[i];
- **B**: prefix[i + 1] = prefix[i] + arr[i];
- **C**: prefix[i] = prefix[i + 1] + arr[i];
- **D**: prefix[i + 1] = prefix[i] + arr[i + 1];

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답 인덱스:** 1
<!-- ANSWER_END -->

### Q7. Code 3. prefix 채우기

아래 코드에서 주석 위치에 들어갈 **한 줄**을 작성하세요.
(prefix 배열은 길이가 n+1이고 prefix[0]=0입니다.)

```c
prefix[0] = 0;
for (int i = 0; i < n; i++) {
    // TODO: prefix[i+1]을 채우는 한 줄을 작성하시오.
}
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
prefix[i + 1] = prefix[i] + arr[i];
```
<!-- ANSWER_END -->

### Q8. Short 2. prefix로 구간합

prefix 배열이 아래와 같을 때, l=1, r=2 (0-index) 구간의 합은 얼마인가요?
(구간합 공식: sum = prefix[r+1] - prefix[l])

```c
int prefix[5] = {0, 2, 5, 9, 10};
int l = 1;
int r = 2;
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답:** `7`
<!-- ANSWER_END -->

### Q9. Code 4. prefix로 구간합 한 줄

아래 코드에서 주석 위치에 들어갈 **한 줄**을 작성하세요.
(0-index, l~r 구간합을 계산해야 합니다.)

```c
int sum_val = 0;
// TODO: prefix를 이용해 l~r 구간합을 sum_val에 저장하는 한 줄을 작성하시오.
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
sum_val = prefix[r + 1] - prefix[l];
```
<!-- ANSWER_END -->
