---
id: "c_lv10_array_b03"
title: "C 배열 Lv10 기초 3회차"
categoryId: "c_array"
availableLanguages: ["c"]
---

# C 배열 Lv10 기초 3회차

### Q1. MCQ 1. 선형 탐색

다음 중 배열 arr에서 값 x가 **존재하는지** 확인해 found를 0/1로 만드는 코드로 올바른 것을 고르세요.
(arr은 길이가 n인 int 배열, x는 int이며, 처음 찾으면 바로 종료해도 됩니다.)

- **A**: int found = 0; for (int i = 0; i < n; i++) {     if (arr[i] == x) found = 1; } // found 사용
- **B**: int found = 0; for (int i = 0; i < n; i++) {     if (arr[i] == x) { found = 1; break; } } // found 사용
- **C**: int found = 1; for (int i = 0; i < n; i++) {     if (arr[i] == x) found = 0; } // found 사용
- **D**: int found = 0; for (int i = 0; i <= n; i++) {     if (arr[i] == x) { found = 1; break; } } // found 사용

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답 인덱스:** 1
<!-- ANSWER_END -->

### Q2. Short 1. 첫 등장 인덱스

다음 배열에서 값 x=7이 **처음 등장하는 인덱스**를 쓰세요. (0-index)

```c
int arr[5] = {5, 7, 2, 7, 9};
int x = 7;
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답:** `1`
<!-- ANSWER_END -->

### Q3. Code 1. 찾으면 for문 종료

아래 코드에서 주석 위치에 들어갈 **한 줄**을 작성하세요.
(x를 찾았을 때 for문을 즉시 종료해야 합니다.)

```c
for (int i = 0; i < n; i++) {
    if (arr[i] == x) {
        // TODO: for문을 즉시 종료하는 한 줄을 작성하시오.
    }
}
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
break;
```
<!-- ANSWER_END -->

### Q4. MCQ 2. 역순 출력 for문

다음 중 배열 arr의 원소를 **뒤에서부터** 한 줄에 하나씩 출력하는 for문으로 올바른 것을 고르세요.
(arr은 길이가 n인 int 배열입니다.)

- **A**: for (int i = n; i >= 0; i--)     printf("%d\n", arr[i]);
- **B**: for (int i = n - 1; i >= 0; i--)     printf("%d\n", arr[i]);
- **C**: for (int i = 0; i < n; i--)     printf("%d\n", arr[i]);
- **D**: for (int i = n - 1; i > 0; i--)     printf("%d\n", arr[i]);

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답 인덱스:** 1
<!-- ANSWER_END -->

### Q5. Code 2. 역순 출력 for문 한 줄

아래 코드에서 주석 위치에 들어갈 **for문 한 줄**을 작성하세요.
(i가 n-1부터 0까지 1씩 감소해야 합니다.)

```c
int n = /* 배열 길이 */;
// TODO: i가 n-1부터 0까지 1씩 감소하는 for문 한 줄을 작성하시오.
    printf("%d\n", arr[i]);
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
for (int i = n - 1; i >= 0; i--)
```
<!-- ANSWER_END -->

### Q6. Short 2. swap 후 배열 상태

아래 코드 실행 후, 배열 arr의 내용을 `arr[0] arr[1] arr[2] arr[3]` 순서로 공백으로 구분해 쓰세요.

```c
int arr[4] = {1, 2, 3, 4};
int tmp = arr[0];
arr[0] = arr[3];
arr[3] = tmp;
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답:** `4 2 3 1`
<!-- ANSWER_END -->

### Q7. MCQ 3. 배열을 뒤집는 코드

다음 중 배열 arr를 **제자리(in-place)**에서 뒤집는 코드로 올바른 것을 고르세요.
(arr은 길이가 n인 int 배열입니다.)

- **A**: for (int i = 0; i < n; i++) {     int tmp = arr[i];     arr[i] = arr[n - 1 - i];     arr[n - 1 - i] = tmp; }
- **B**: for (int i = 0; i < n / 2; i++) {     int tmp = arr[i];     arr[i] = arr[n - 1 - i];     arr[n - 1 - i] = tmp; }
- **C**: for (int i = 0; i <= n / 2; i++) {     int tmp = arr[i];     arr[i] = arr[n - i];     arr[n - i] = tmp; }
- **D**: for (int i = 0; i < n / 2; i++) {     int tmp = arr[i];     arr[i] = arr[n - i];     arr[n - i] = tmp; }

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답 인덱스:** 1
<!-- ANSWER_END -->

### Q8. Code 3. 최댓값 인덱스 갱신

아래 코드에서 주석 위치에 들어갈 **한 줄**을 작성하세요.
(arr[i]가 더 크면 max_idx를 i로 갱신해야 합니다.)

```c
int max_idx = 0;
for (int i = 1; i < n; i++) {
    if (arr[i] > arr[max_idx]) {
        // TODO: max_idx를 갱신하는 한 줄을 작성하시오.
    }
}
```

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **모범 코드:**
```c
max_idx = i;
```
<!-- ANSWER_END -->

### Q9. MCQ 4. 배열 복사

다음 중 배열 a의 내용을 **독립적인 새 배열 b**로 복사하는 코드로 올바른 것을 고르세요.
(복사 후 a를 바꿔도 b가 영향을 받지 않아야 합니다.)

- **A**: int *b = a;
- **B**: int *b = (int*)malloc(sizeof(int) * n); for (int i = 0; i < n; i++) b[i] = a[i];
- **C**: int *b = (int*)malloc(sizeof(int) * n); b = a;
- **D**: int *b = (int*)malloc(sizeof(int) * (n - 1)); for (int i = 0; i < n; i++) b[i] = a[i];

---
<!-- ANSWER_START -->
### [답안 및 해설]
- **정답 인덱스:** 1
<!-- ANSWER_END -->
