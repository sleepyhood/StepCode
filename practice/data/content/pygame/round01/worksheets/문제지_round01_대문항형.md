---
id: "py_pygame_round01_problem_sheet"
contentType: "worksheet"
track: "pygame"
lang: "python"
categoryId: "py_pygame"
title: "Python Pygame Round01 대문항형 문제지"
round: 1
difficulty: "application"
status: "draft"
audience: "common"
printDefault: false
---

# Python Pygame Round01 대문항형 문제지

## 안내

이 문제지는 `6개의 대문항`으로 구성되어 있습니다.
각 대문항에는 `3개의 소문항`이 있으며,
하나의 실행 코드 흐름 안에서 문제를 해결하도록 설계되었습니다.

문제를 풀 때는 아래 순서를 따르세요.

1. 코드를 먼저 실행합니다.
2. 화면이나 오류 메시지를 관찰합니다.
3. 필요한 코드를 수정합니다.
4. 다시 실행하여 결과를 확인합니다.
5. 수정한 이유와 결과를 함께 적습니다.

답안을 적을 때는 가능하면 아래 내용을 함께 쓰세요.

- 무엇이 문제였는가
- 어떤 코드를 수정했는가
- 수정 후 무엇이 달라졌는가

---

## 1번. 창 만들기와 기본 화면 구성

실행 파일:

- `problems/problem01.py`

다음은 `pygame`으로 창을 만들고,
기본 도형을 화면에 출력하는 코드입니다.
현재 코드에는 실행 오류의 원인이 하나 들어 있고,
도형의 위치도 요구 조건과 다를 수 있습니다.
아래 물음에 답하시오.

제시 코드:

```python
import sys
import pygame
from pygame.locals import QUIT

pygame.init()
SURFACE = pygame.display.set_mode((400, 300))
pygame.display.set_caption("round01 problem01")


def main():
    while True:
        for event in pygame.event.get():
            if event.type == QUIT:
                pygame.quit()
                sys.exit()

        # 문제 1.1: 왜 실행 오류가 나는지 찾고 고치세요.
        SURFACE.fill((255, 255))

        # 문제 1.2: 사각형이 화면 중앙에 오도록 좌표를 수정하세요.
        pygame.draw.rect(SURFACE, (255, 120, 120), (0, 0, 160, 90))
        pygame.display.update()


if __name__ == "__main__":
    main()
```

### 1.1

현재 코드에서 실행 오류가 발생하는 이유를 찾고,
잘못된 부분을 고쳐 쓰시오.

답:

`______________________________________________________________________________`

`______________________________________________________________________________`

`______________________________________________________________________________`

### 1.2

현재 도형이 왼쪽 위 기준으로 배치되어 있다.
도형이 화면의 중앙에 오도록 좌표를 수정하시오.

수정 전 위치 설명:

`______________________________________________________________________________`

수정한 코드 또는 값:

`______________________________________________________________________________`

수정 후 변화:

`______________________________________________________________________________`

### 1.3

창 크기 또는 배경색 규칙을 바꾸어,
화면이 더 보기 좋게 보이도록 수정하시오.
수정한 값과 그 이유를 함께 적으시오.

수정한 값:

`______________________________________________________________________________`

수정 이유:

`______________________________________________________________________________`

결과 설명:

`______________________________________________________________________________`

---

## 2번. 도형 그리기 순서와 겹침

실행 파일:

- `problems/problem02.py`

다음 코드는 여러 도형을 화면에 그리는 예시이다.
도형의 위치와 그리는 순서에 따라
화면에서 보이는 결과가 달라진다.
아래 물음에 답하시오.

제시 코드:

```python
import sys
import pygame
from pygame.locals import QUIT

pygame.init()
SURFACE = pygame.display.set_mode((500, 350))
pygame.display.set_caption("round01 problem02")


def main():
    while True:
        for event in pygame.event.get():
            if event.type == QUIT:
                pygame.quit()
                sys.exit()

        SURFACE.fill((245, 245, 245))
        pygame.draw.rect(SURFACE, (255, 120, 120), (140, 90, 170, 130))
        pygame.draw.circle(SURFACE, (80, 140, 255), (250, 170), 85)
        pygame.display.update()


if __name__ == "__main__":
    main()
```

### 2.1

두 도형이 서로 겹칠 때,
어떤 도형이 앞에 보이는지 관찰하고 그 이유를 서술하시오.

관찰 결과:

`______________________________________________________________________________`

이유:

`______________________________________________________________________________`

### 2.2

도형이 겹친 상태에서,
제시된 결과와 같아지도록 `draw` 코드의 순서
또는 좌표를 수정하시오.

수정한 부분:

`______________________________________________________________________________`

수정 이유:

`______________________________________________________________________________`

수정 후 결과:

`______________________________________________________________________________`

### 2.3

새 도형 하나를 추가하고,
그 도형이 항상 가장 위에 보이도록 코드를 작성하시오.

추가한 도형:

`______________________________________________________________________________`

작성한 코드:

`______________________________________________________________________________`

확인한 결과:

`______________________________________________________________________________`

---

## 3번. 이벤트 처리와 정상 종료

실행 파일:

- `problems/problem03.py`

다음 코드는 화면은 열리지만,
종료 버튼을 눌렀을 때 정상적으로 닫히지 않는 문제가 있다.
아래 물음에 답하시오.

제시 코드:

```python
import sys
import pygame
from pygame.locals import QUIT

pygame.init()
SURFACE = pygame.display.set_mode((400, 300))
pygame.display.set_caption("round01 problem03")


def main():
    while True:
        SURFACE.fill((20, 20, 20))
        pygame.draw.circle(SURFACE, (255, 210, 80), (200, 150), 60)

        if False:
            pygame.quit()
            sys.exit()

        pygame.display.update()


if __name__ == "__main__":
    main()
```

### 3.1

현재 창의 `X` 버튼을 눌러도 종료되지 않는 이유를 설명하시오.

답:

`______________________________________________________________________________`

`______________________________________________________________________________`

### 3.2

창이 정상적으로 닫히도록 이벤트 처리 코드를 수정하시오.

수정한 코드:

`______________________________________________________________________________`

`______________________________________________________________________________`

수정 후 확인 결과:

`______________________________________________________________________________`

### 3.3

특정 키를 누르면 화면 색이나 도형 색이 바뀌도록
입력 규칙을 하나 추가하시오.

추가한 규칙:

`______________________________________________________________________________`

추가한 코드:

`______________________________________________________________________________`

동작 결과:

`______________________________________________________________________________`

---

## 4번. 화면 갱신과 속도 제어

실행 파일:

- `problems/problem04.py`

다음 코드는 색이 변하고 도형이 움직이도록 작성되어 있다.
그러나 화면 갱신 또는 속도 조절과 관련된 개념을
정확히 이해해야 원하는 결과를 얻을 수 있다.
아래 물음에 답하시오.

제시 코드:

```python
import sys
import pygame
from pygame.locals import QUIT

pygame.init()
SURFACE = pygame.display.set_mode((500, 240))
pygame.display.set_caption("round01 problem04")
FPSCLOCK = pygame.time.Clock()


def main():
    xpos = 0
    color_value = 0

    while True:
        for event in pygame.event.get():
            if event.type == QUIT:
                pygame.quit()
                sys.exit()

        xpos += 4
        if xpos > 540:
            xpos = -40

        color_value += 3
        if color_value > 255:
            color_value = 0

        SURFACE.fill((color_value, 100, 255 - color_value))
        pygame.draw.rect(SURFACE, (50, 120, 255), (xpos, 120, 40, 40))

        # 문제 4.1: 화면 변화가 보이도록 빠진 한 줄을 넣으세요.
        # 문제 4.2, 4.3: tick 값을 바꿔 움직임을 비교하세요.
        FPSCLOCK.tick(60)


if __name__ == "__main__":
    main()
```

### 4.1

색 값은 바뀌지만 화면 변화가 보이지 않는 이유를 찾고,
필요한 한 줄을 추가하시오.

문제 원인:

`______________________________________________________________________________`

추가한 코드:

`______________________________________________________________________________`

수정 후 결과:

`______________________________________________________________________________`

### 4.2

`tick(1)`, `tick(10)`, `tick(60)`, `tick(120)`으로 바꾸어 실행해 보고,
움직임이 어떻게 달라지는지 비교하여 쓰시오.

비교 결과:

`______________________________________________________________________________`

`______________________________________________________________________________`

`______________________________________________________________________________`

### 4.3

사각형이 너무 빠르거나 느리지 않게 움직이도록
`tick` 값 또는 이동값을 조절하시오.
자신이 선택한 값과 그 이유를 쓰시오.

선택한 값:

`______________________________________________________________________________`

이유:

`______________________________________________________________________________`

최종 결과:

`______________________________________________________________________________`

---

## 5번. 회전과 중심 좌표

실행 파일:

- `problems/problem05.py`

다음 코드는 하나의 이미지를 계속 회전시키는 프로그램이다.
하지만 회전 결과가 의도한 위치를 중심으로
움직이지 않는 문제가 있다.
아래 물음에 답하시오.

제시 코드:

```python
import sys
import pygame
from pygame.locals import QUIT

pygame.init()
SURFACE = pygame.display.set_mode((520, 320))
FPSCLOCK = pygame.time.Clock()


def make_sprite():
    sprite = pygame.Surface((140, 80), pygame.SRCALPHA)
    pygame.draw.rect(sprite, (255, 140, 120), (10, 10, 120, 60), border_radius=12)
    pygame.draw.circle(sprite, (255, 230, 90), (35, 40), 14)
    pygame.draw.circle(sprite, (90, 180, 255), (105, 40), 14)
    return sprite


def main():
    sprite = make_sprite()
    theta = 0

    while True:
        for event in pygame.event.get():
            if event.type == QUIT:
                pygame.quit()
                sys.exit()

        theta += 3
        SURFACE.fill((25, 25, 30))
        rotated = pygame.transform.rotate(sprite, theta)
        SURFACE.blit(rotated, (0, 0))
        pygame.display.update()
        FPSCLOCK.tick(60)


if __name__ == "__main__":
    main()
```

### 5.1

현재 스프라이트가 회전할 때
어떤 문제가 생기는지 관찰하여 서술하시오.

답:

`______________________________________________________________________________`

`______________________________________________________________________________`

### 5.2

회전한 이미지가 화면 가운데를 기준으로 돌도록
코드를 수정하시오.

수정한 코드:

`______________________________________________________________________________`

`______________________________________________________________________________`

수정 후 결과:

`______________________________________________________________________________`

### 5.3

회전 속도나 시작 위치를 바꾸어
다른 움직임이 되도록 수정하시오.

바꾼 값:

`______________________________________________________________________________`

의도:

`______________________________________________________________________________`

결과:

`______________________________________________________________________________`

---

## 6번. 마우스 입력과 그리기 규칙

실행 파일:

- `problems/problem06.py`

다음 코드는 마우스 움직임을 이용해 점을 그리는
간단한 그림판 예제이다.
현재는 의도하지 않은 상황에서도 점이 계속 그려진다.
아래 물음에 답하시오.

제시 코드:

```python
import sys
import pygame
from pygame.locals import QUIT, MOUSEMOTION

pygame.init()
SURFACE = pygame.display.set_mode((600, 420))
FPSCLOCK = pygame.time.Clock()


def main():
    mouse_positions = []

    while True:
        for event in pygame.event.get():
            if event.type == QUIT:
                pygame.quit()
                sys.exit()
            elif event.type == MOUSEMOTION:
                mouse_positions.append(event.pos)

        SURFACE.fill((255, 255, 255))

        for pos in mouse_positions:
            pygame.draw.circle(SURFACE, (20, 20, 20), pos, 5)

        pygame.display.update()
        FPSCLOCK.tick(60)


if __name__ == "__main__":
    main()
```

### 6.1

마우스를 움직이기만 해도 점이 그려지는 이유를 설명하시오.

답:

`______________________________________________________________________________`

`______________________________________________________________________________`

### 6.2

마우스를 클릭한 상태에서만 점이 그려지도록
조건을 수정하시오.

수정한 코드:

`______________________________________________________________________________`

`______________________________________________________________________________`

수정 후 결과:

`______________________________________________________________________________`

### 6.3

점의 색, 크기, 또는 지우기 규칙 중 하나를 추가하여
기능을 확장하시오.

추가한 기능:

`______________________________________________________________________________`

작성한 코드:

`______________________________________________________________________________`

실행 결과:

`______________________________________________________________________________`

---

## 최종 점검

1. 가장 먼저 수정해야 한다고 느낀 문제는 무엇이었나요?

`______________________________________________________________________________`

2. 가장 어려웠던 문항은 무엇이었나요?

`______________________________________________________________________________`

3. 오늘 문제를 풀면서 새롭게 이해한 `pygame` 개념 한 가지를 쓰시오.

`______________________________________________________________________________`

4. 다시 비슷한 문제가 나온다면,
먼저 확인할 코드 위치는 어디인가요?

`______________________________________________________________________________`
