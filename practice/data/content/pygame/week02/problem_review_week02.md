# Pygame Week02 문제 검수 원본

> 이 문서는 `week02` 웹 노출 구조에 맞춰 정리된 검수 원본입니다.
> 원본 제작 문맥과 코드 캡션에는 `round01` 표기가 남아 있을 수 있지만,
> 현재 웹/인덱스 기준 운영 루트는 `practice/data/content/pygame/week02/`입니다.

이 문서는 `모듈 02 · 이미지 출력과 회전 중심` 문제지를 검수하기 위한 원본 문서이다.
학생에게는 별도의 문제 파일을 제공하지 않고,
제시된 코드를 직접 작성하게 하는 구성을 기준으로 한다.

`week02`는 `round01`의 기존 5번 문항을
모듈 02 독립 세트로 확장한 검수 원본이다.
핵심은 `이미지를 어디에 그리는지`, `왜 회전 결과가 어색한지`,
`중심 좌표를 어떻게 보정하는지`, `숫자 하나가 회전 결과를 어떻게 바꾸는지`를
코드 기준으로 읽고 수정하는 데 있다.

이 문서는 공통 원칙 문서인
`../pygame_문제_제작_운영원칙.md`를 따른다.
이 문서에는 모듈 02 세부 문항과 검수 내용을 중심으로 기록한다.

내부 제작 기준 자료:

- `../source/md_1회차_파이썬으로_게임만들기.md`
- `problems/problem01.py`
- `problems/problem02.py`
- `problems/problem03.py`
- `problems/problem04.py`
- `problems/problem05.py`
- `reference_images/problem05_correct.png`
- `reference_images/problem05_correct.gif`

---

## 재구성 원칙

현재 `week02` 검수 범위는 아래 4개 대문항으로 고정한다.

1. 이미지 출력 위치와 좌표 기준
2. 회전 결과가 어색한 이유 찾기
3. `get_rect(center=...)`와 중심 좌표 읽기
4. 회전 속도와 결과 비교

운영 목표:

- `blit`의 위치 인자가 화면 결과를 어떻게 바꾸는지 확인하게 한다.
- 회전 자체와 출력 위치 보정을 분리해서 이해하게 한다.
- `get_rect(center=...)`가 왜 필요한지 설명하게 한다.
- 각도 증가량 같은 숫자 하나가 회전 결과를 어떻게 바꾸는지 해석하게 한다.

제외 또는 후순위:

- `rotozoom`
- 외부 이미지 자산 로딩 심화
- 다중 스프라이트 관리
- 텍스트 회전과 크기 변경

이 항목들은 이후 확장 응용에서 별도로 다룬다.

---

## 작성 규칙

소문항은 반드시 아래 순서를 유지한다.

1. 난이도
2. 형식
3. 출제 의도
4. 문제
5. 정답
6. 해설
7. 부분 정답 기준
8. 실제 실행 확인 결과
9. 검수 체크

형식 규칙:

- `코드 입력형`: 정답이 코드 한 줄 또는 짧은 블록
- `빈칸형`: 정답이 함수명, 상수, 숫자, 코드 조각
- `복수정답 객관식형`: 정답이 정해진 선택 조합

모든 대문항은 `누적풀이`를 기준으로 설계한다.
뒤 문항은 앞 문항의 수정 결과를 이어받는 것을 기본으로 한다.

---

## 1번. 이미지 출력 위치와 좌표 기준

제작 기준 코드:

- `problems/problem01.py`

확인 개념:

- `SURFACE.blit`
- `(x, y)` 좌표
- 화면 안/밖 출력
- 중심 근처 이동

코드 설명:

이 프로그램은 스프라이트 하나를 회전 없이 화면에 출력하는 코드이다.
현재는 이미지가 `(0, 0)`에 고정되어 있어
화면 구석에 붙어 보인다.
이미지 출력 위치와 좌표 기준을 먼저 읽고,
이후 회전 문제로 넘어가기 위한 기초를 확인한다.

제시 코드:

```python
import sys
import pygame
from pygame.locals import QUIT

pygame.init()
SURFACE = pygame.display.set_mode((520, 320))
pygame.display.set_caption("week02 problem01")


def make_sprite():
    sprite = pygame.Surface((140, 80), pygame.SRCALPHA)
    pygame.draw.rect(sprite, (255, 140, 120), (10, 10, 120, 60), border_radius=12)
    pygame.draw.circle(sprite, (255, 230, 90), (35, 40), 14)
    pygame.draw.circle(sprite, (90, 180, 255), (105, 40), 14)
    return sprite


def main():
    sprite = make_sprite()

    while True:
        for event in pygame.event.get():
            if event.type == QUIT:
                pygame.quit()
                sys.exit()

        SURFACE.fill((25, 25, 30))

        # BLOCK_A_START
        # 문제 1.2: 이미지를 화면 가운데 근처로 옮기세요.
        SURFACE.blit(sprite, (0, 0))
        # BLOCK_A_END

        pygame.display.update()


if __name__ == "__main__":
    main()
```

참고 이미지:

아래 이미지는 코드가 `올바르게 수정되어 정상 작동할 경우`를
기준으로 한 참고 화면이다.

![1번 정상 작동 참고 이미지](./reference_images/problem05_correct.png)

### 1.1

난이도:

- 하

형식:

- 빈칸형

출제 의도:

- `blit`의 위치 인자를 직접 읽게 한다.

문제:

현재 스프라이트가 기준으로 삼고 있는 좌표를 쓰시오.

정답:

- `(0, 0)`

해설:

- 제시 코드는 `SURFACE.blit(sprite, (0, 0))`을 사용한다.
- 따라서 이미지의 왼쪽 위 기준 위치는 `(0, 0)`이다.

부분 정답 기준:

- 없음. `(0, 0)`만 정답 처리

실제 실행 확인 결과:

- 제시 코드 기준으로 스프라이트는 화면 왼쪽 위 구석에 붙어 보인다.

검수 체크:

- 좌표 읽기 문제로 기능하는지 확인

### 1.2

난이도:

- 중

형식:

- 코드 입력형

출제 의도:

- 한 줄 수정으로 출력 위치를 바꾸게 한다.

문제:

창 크기가 `(520, 320)`이고 스프라이트 크기가 `(140, 80)`일 때,
이미지가 화면 가운데 근처에 오도록
`blit` 한 줄을 다시 쓰시오.

정답:

```python
SURFACE.blit(sprite, (190, 120))
```

해설:

- 가로 중앙 정렬을 위해 `520 - 140 = 380`, 그 절반은 `190`이다.
- 세로 중앙 정렬을 위해 `320 - 80 = 240`, 그 절반은 `120`이다.

부분 정답 기준:

- `SURFACE.blit(sprite, (...))` 형태를 유지하면 부분 정답
- x 또는 y 하나만 맞으면 부분 정답
- `(190, 120)`이면 정답

실제 실행 확인 결과:

- 제시 코드의 `(0, 0)`을 `(190, 120)`으로 바꾸면 이미지가 화면 가운데 근처로 이동한다.

검수 체크:

- 정답이 한 줄로 고정되는지 확인

### 1.3

난이도:

- 하

형식:

- 빈칸형

출제 의도:

- 화면 중앙 배치 좌표 계산을 숫자로 직접 확인하게 한다.

문제:

1.2의 좌표를 직접 계산하려고 한다.
아래 빈칸에 들어갈 숫자를 순서대로 쓰시오.

```python
SURFACE.blit(sprite, (____, ____))
```

정답:

- `190, 120`

해설:

- 이미지의 왼쪽 위 좌표를 기준으로 중앙 배치를 계산한다.
- 창 크기와 스프라이트 크기를 함께 보고 계산해야 한다.

부분 정답 기준:

- 하나만 맞으면 부분 정답
- 둘 다 맞으면 정답

실제 실행 확인 결과:

- 창 `(520, 320)`과 이미지 `(140, 80)` 기준으로 중앙 배치 좌표는 `(190, 120)`이다.

검수 체크:

- 1.2와 1.3의 좌표 기준이 일치하는지 확인

### 1.4

난이도:

- 중

형식:

- 복수정답 객관식형

출제 의도:

- 좌표 변경 뒤 실행 결과를 코드 기준으로 판단하게 한다.

문제:

1.2를 반영한 상태라고 가정한다.
실행 후 옳은 것을 모두 고른 것은?

- ㄱ. 이미지가 이전보다 오른쪽으로 이동한다.
- ㄴ. 이미지가 이전보다 아래쪽으로 이동한다.
- ㄷ. 이미지가 자동으로 회전하기 시작한다.
- ㄹ. 배경색은 그대로다.

정답:

- `ㄱ, ㄴ, ㄹ`

해설:

- 위치 좌표만 바꾸었으므로 이미지의 출력 위치만 달라진다.
- 회전 코드는 없으므로 자동 회전은 발생하지 않는다.
- 배경색을 바꾸는 코드도 없으므로 기존과 같다.

부분 정답 기준:

- `ㄱ, ㄴ` 또는 `ㄱ, ㄹ`이면 부분 정답
- `ㄱ, ㄴ, ㄹ`이면 정답

실제 실행 확인 결과:

- 이미지는 중앙 근처로 이동하고 배경색은 그대로 유지된다.

검수 체크:

- 결과 예측 문항이 위치 이동만 묻도록 유지되는지 확인

---

## 2번. 회전 결과가 어색한 이유 찾기

제작 기준 코드:

- `problems/problem02.py`

확인 개념:

- `pygame.transform.rotate`
- 회전 전후 이미지 크기 변화
- 회전 후 `(0, 0)` 출력 문제
- 좌상단 기준 회전처럼 보이는 현상

코드 설명:

이 프로그램은 하나의 스프라이트를 계속 회전시키는 코드이다.
현재는 회전한 이미지를 `(0, 0)`에 바로 그리기 때문에
중심을 기준으로 도는 것처럼 보이지 않는다.
회전 결과가 왜 어색한지 먼저 찾고,
회전과 출력 위치 보정을 분리해서 이해하도록 설계한다.

제시 코드:

```python
import sys
import pygame
from pygame.locals import QUIT

pygame.init()
SURFACE = pygame.display.set_mode((520, 320))
pygame.display.set_caption("week02 problem02")
FPSCLOCK = pygame.time.Clock()


def make_sprite():
    sprite = pygame.Surface((140, 80), pygame.SRCALPHA)
    pygame.draw.rect(sprite, (255, 140, 120), (10, 10, 120, 60), border_radius=12)
    pygame.draw.circle(sprite, (255, 230, 90), (35, 40), 14)
    pygame.draw.circle(sprite, (90, 180, 255), (105, 40), 14)
    return sprite


def main():
    sprite = make_sprite()
    theta = 0

    while True:
        for event in pygame.event.get():
            if event.type == QUIT:
                pygame.quit()
                sys.exit()

        # BLOCK_A_START
        theta += 3
        SURFACE.fill((25, 25, 30))
        rotated = pygame.transform.rotate(sprite, theta)
        # 문제 2.2: 현재 출력 기준 좌표를 확인하세요.
        SURFACE.blit(rotated, (0, 0))
        # BLOCK_A_END

        pygame.display.update()
        FPSCLOCK.tick(60)


if __name__ == "__main__":
    main()
```

참고 이미지:

아래 이미지는 코드가 `올바르게 수정되어 정상 작동할 경우`를
기준으로 한 참고 화면이다.

![2번 정상 작동 참고 GIF](./reference_images/problem05_correct.gif)

### 2.1

난이도:

- 중

형식:

- 복수정답 객관식형

출제 의도:

- 회전 결과가 어색한 이유를 위치 보정 관점에서 읽게 한다.

문제:

제시된 코드에서 회전 결과가 어색하게 보이는 이유로
옳은 것을 모두 고른 것은?

- ㄱ. 회전된 이미지를 `(0, 0)`에 바로 출력한다.
- ㄴ. 회전 결과에 맞는 새 rect를 잡지 않았다.
- ㄷ. `pygame.display.update()`가 없어서 회전이 안 된다.
- ㄹ. `theta` 값이 음수가 아니라서 회전이 어색하다.

정답:

- `ㄱ, ㄴ`

해설:

- 핵심 문제는 회전된 이미지를 그대로 `(0, 0)`에 출력하는 것과
  중심 보정이 없는 것이다.
- `update()`는 이미 존재하므로 회전이 보이는 조건은 갖추고 있다.

부분 정답 기준:

- `ㄱ`만 고르면 부분 정답
- `ㄱ, ㄴ`이면 정답

실제 실행 확인 결과:

- 제시 코드 기준으로 이미지는 화면 왼쪽 위를 기준으로 흔들리듯 회전한다.

검수 체크:

- 회전 문제의 원인이 위치 보정과 연결되는지 확인

### 2.2

난이도:

- 하

형식:

- 빈칸형

출제 의도:

- 현재 출력 기준 좌표를 정확히 확인하게 한다.

문제:

회전된 이미지가 기준으로 삼고 있는 현재 출력 좌표를 쓰시오.

정답:

- `(0, 0)`

해설:

- 제시 코드는 `SURFACE.blit(rotated, (0, 0))`을 사용한다.

부분 정답 기준:

- 없음. `(0, 0)`만 정답 처리

실제 실행 확인 결과:

- 회전된 이미지도 `(0, 0)` 기준으로 출력된다.

검수 체크:

- 2.1과 2.2의 논리 연결이 자연스러운지 확인

### 2.3

난이도:

- 중상

형식:

- 빈칸형

출제 의도:

- 중심 보정의 핵심 코드 조각을 직접 복구하게 한다.

문제:

2.2에서 확인한 `(0, 0)` 기준 출력을 고치기 위해
회전한 이미지의 중심을 `(260, 160)`으로 맞추는
아래 빈칸을 채우시오.

정답:

```python
rotated_rect = rotated.get_rect(________)
SURFACE.blit(rotated, ________)
```

- `center=(260, 160)`
- `rotated_rect`

해설:

- 회전된 결과물에 맞는 새 rect를 만든 뒤
  그 rect의 중심을 화면 중앙으로 잡아야 한다.

부분 정답 기준:

- 첫 번째 빈칸만 맞으면 부분 정답
- 두 빈칸을 모두 맞히면 정답

실제 실행 확인 결과:

- 중심 보정을 적용하면 회전 결과가 화면 가운데를 기준으로 안정적으로 보인다.

검수 체크:

- 정답이 하나로 고정되는지 확인

### 2.4

난이도:

- 중

형식:

- 복수정답 객관식형

출제 의도:

- 중심 보정 후 실행 결과를 종합 판단하게 한다.

문제:

2.3을 반영한 상태라고 가정한다.
실행 후 옳은 것을 모두 고른 것은?

- ㄱ. 이미지가 화면 가운데를 기준으로 도는 것처럼 보인다.
- ㄴ. 회전 자체는 계속된다.
- ㄷ. 이미지는 계속 `(0, 0)`에서만 돈다.
- ㄹ. 배경색은 그대로다.

정답:

- `ㄱ, ㄴ, ㄹ`

해설:

- 중심 보정은 회전 위치를 바로잡는 것이지 회전 자체를 멈추지 않는다.
- 출력 위치는 개선되지만 배경색 코드는 그대로다.

부분 정답 기준:

- `ㄱ, ㄴ` 또는 `ㄱ, ㄹ`이면 부분 정답
- `ㄱ, ㄴ, ㄹ`이면 정답

실제 실행 확인 결과:

- 중심 보정 후에도 이미지는 계속 회전하고, 회전 중심만 화면 가운데로 안정된다.

검수 체크:

- 수정 후 동작 설명이 실제 결과와 일치하는지 확인

---

## 3번. `get_rect(center=...)`와 중심 좌표 읽기

제작 기준 코드:

- `problems/problem03.py`

확인 개념:

- `get_rect()`
- `rect.center`
- 창 중심 좌표 계산
- 회전 이미지와 위치 객체의 연결

코드 설명:

이 프로그램은 회전된 이미지를 화면 가운데에 두고 싶지만,
중심 좌표가 잘못되어 한쪽으로 치우친 상태의 코드이다.
이 대문항에서는 `get_rect(center=...)`를 단순 암기하지 않고,
`rect`가 위치 역할을 한다는 점을 읽게 한다.

제시 코드:

```python
import sys
import pygame
from pygame.locals import QUIT

pygame.init()
SURFACE = pygame.display.set_mode((520, 320))
pygame.display.set_caption("week02 problem03")
FPSCLOCK = pygame.time.Clock()


def make_sprite():
    sprite = pygame.Surface((140, 80), pygame.SRCALPHA)
    pygame.draw.rect(sprite, (255, 140, 120), (10, 10, 120, 60), border_radius=12)
    pygame.draw.circle(sprite, (255, 230, 90), (35, 40), 14)
    pygame.draw.circle(sprite, (90, 180, 255), (105, 40), 14)
    return sprite


def main():
    sprite = make_sprite()
    theta = 0

    while True:
        for event in pygame.event.get():
            if event.type == QUIT:
                pygame.quit()
                sys.exit()

        theta += 3
        SURFACE.fill((25, 25, 30))
        rotated = pygame.transform.rotate(sprite, theta)

        # BLOCK_A_START
        rotated_rect = rotated.get_rect()
        # 문제 3.1, 3.4: 중심 좌표를 올바르게 고치세요.
        rotated_rect.center = (180, 120)
        SURFACE.blit(rotated, rotated_rect)
        # BLOCK_A_END

        pygame.display.update()
        FPSCLOCK.tick(60)


if __name__ == "__main__":
    main()
```

### 3.1

난이도:

- 중

형식:

- 빈칸형

출제 의도:

- 중심 좌표를 숫자로 직접 읽고 수정하게 한다.

문제:

창 중앙이 `(260, 160)`일 때
아래 빈칸에 들어갈 숫자를 순서대로 쓰시오.

```python
rotated_rect.center = (____, ____)
```

정답:

- `260, 160`

해설:

- 창 크기 `(520, 320)` 기준 중심 좌표는 `(260, 160)`이다.
- `rect.center`는 이미지의 중심 위치를 뜻한다.

부분 정답 기준:

- 하나만 맞으면 부분 정답
- 둘 다 맞으면 정답

실제 실행 확인 결과:

- 제시 코드의 `(180, 120)`은 화면 중앙보다 왼쪽 위로 치우친 값이다.

검수 체크:

- 중심 좌표 계산이 창 크기와 일치하는지 확인

### 3.2

난이도:

- 하

형식:

- 빈칸형

출제 의도:

- `blit`의 위치 인자로 rect 변수를 다시 확인하게 한다.

문제:

아래 코드의 빈칸에 들어갈 변수명을 쓰시오.

```python
SURFACE.blit(rotated, ________)
```

정답:

- `rotated_rect`

해설:

- `rotated_rect`는 회전된 이미지의 위치 역할을 하는 rect 변수다.

부분 정답 기준:

- 없음. `rotated_rect`만 정답 처리

실제 실행 확인 결과:

- 제시 코드도 `SURFACE.blit(rotated, rotated_rect)` 구조를 사용한다.

검수 체크:

- 변수명과 역할이 연결되는지 확인

### 3.3

난이도:

- 중

형식:

- 복수정답 객관식형

출제 의도:

- `get_rect(center=...)`의 의미를 설명형으로 검증한다.

문제:

`rotated.get_rect(center=(260, 160))`에 대한 설명으로
옳은 것을 모두 고른 것은?

- ㄱ. 회전된 이미지에 맞는 새 rect를 만든다.
- ㄴ. 그 rect의 중심 좌표를 `(260, 160)`으로 둔다.
- ㄷ. 이미지 색상을 자동으로 바꾼다.
- ㄹ. `blit`에 사용할 위치 정보를 함께 만들 수 있다.

정답:

- `ㄱ, ㄴ, ㄹ`

해설:

- `get_rect(center=...)`는 회전된 이미지의 새 경계 rect를 만들고
  그 중심 좌표를 동시에 설정한다.
- 색상 변경과는 관련이 없다.

부분 정답 기준:

- `ㄱ, ㄴ` 또는 `ㄴ, ㄹ`이면 부분 정답
- `ㄱ, ㄴ, ㄹ`이면 정답

실제 실행 확인 결과:

- 중심 좌표를 함께 설정하면 `blit`에 바로 쓸 수 있는 위치 객체가 만들어진다.

검수 체크:

- `get_rect` 설명이 지나치게 추상적이지 않은지 확인

### 3.4

난이도:

- 중

형식:

- 코드 입력형

출제 의도:

- 한 줄 수정으로 잘못된 중심 좌표를 바로잡게 한다.

문제:

제시 코드에서 회전 이미지를 화면 중앙에 오도록 만들기 위해
`center`를 설정하는 한 줄을 다시 쓰시오.

정답:

```python
rotated_rect.center = (260, 160)
```

해설:

- 이 문항은 `get_rect` 뒤에 오는 중심 좌표 설정 한 줄만 정확히 복구하게 한다.
- 코드 입력형이지만 정답이 한 줄로 고정되어 있다.

부분 정답 기준:

- `rotated_rect.center = (...)` 형태를 유지하면 부분 정답
- `(260, 160)`이면 정답

실제 실행 확인 결과:

- 중심 좌표를 바로잡으면 회전 이미지가 화면 중앙으로 이동한다.

검수 체크:

- 코드 입력형이 한 줄로 고정되는지 확인

---

## 4번. 회전 속도와 결과 비교

제작 기준 코드:

- `problems/problem04.py`

확인 개념:

- `theta += n`
- 회전 속도 변화
- 중심 보정 유지
- 숫자 변경에 따른 결과 비교

코드 설명:

이 프로그램은 중심 보정이 이미 적용된 상태에서
`theta` 증가량만 바꿔 회전 속도를 비교하는 코드이다.
이 대문항에서는 `중심 보정은 유지`하고
`숫자 하나가 결과를 어떻게 바꾸는지`만 분리해서 본다.

제시 코드:

```python
import sys
import pygame
from pygame.locals import QUIT

pygame.init()
SURFACE = pygame.display.set_mode((520, 320))
pygame.display.set_caption("week02 problem04")
FPSCLOCK = pygame.time.Clock()


def make_sprite():
    sprite = pygame.Surface((140, 80), pygame.SRCALPHA)
    pygame.draw.rect(sprite, (255, 140, 120), (10, 10, 120, 60), border_radius=12)
    pygame.draw.circle(sprite, (255, 230, 90), (35, 40), 14)
    pygame.draw.circle(sprite, (90, 180, 255), (105, 40), 14)
    return sprite


def main():
    sprite = make_sprite()
    theta = 0

    while True:
        for event in pygame.event.get():
            if event.type == QUIT:
                pygame.quit()
                sys.exit()

        # BLOCK_A_START
        # 문제 4.1: 더 느리게 회전하도록 숫자를 바꾸세요.
        theta += 3
        SURFACE.fill((25, 25, 30))
        rotated = pygame.transform.rotate(sprite, theta)
        rotated_rect = rotated.get_rect(center=(260, 160))
        SURFACE.blit(rotated, rotated_rect)
        # BLOCK_A_END

        pygame.display.update()
        FPSCLOCK.tick(60)


if __name__ == "__main__":
    main()
```

참고 이미지:

아래 이미지는 코드가 `올바르게 수정되어 정상 작동할 경우`를
기준으로 한 참고 화면이다.

![4번 정상 작동 참고 GIF](./reference_images/problem05_correct.gif)

### 4.1

난이도:

- 하

형식:

- 빈칸형

출제 의도:

- 회전 속도를 숫자 하나로 조절한다는 점을 확인하게 한다.

문제:

현재보다 더 느리게 회전하도록 하려면
아래 빈칸에 들어갈 숫자를 쓰시오.

```python
theta += ____
```

정답:

- `1`

해설:

- 각도 증가량이 작을수록 한 프레임마다 회전하는 양이 줄어든다.
- 따라서 더 느리게 회전하는 것처럼 보인다.

부분 정답 기준:

- 없음. `1`만 정답 처리

실제 실행 확인 결과:

- `theta += 1`로 바꾸면 기존 `theta += 3`보다 더 천천히 회전한다.

검수 체크:

- 숫자 하나 변경으로 결과가 달라지는 구조인지 확인

### 4.2

난이도:

- 중

형식:

- 복수정답 객관식형

출제 의도:

- 회전 속도만 바뀌었을 때의 결과를 분리해서 읽게 한다.

문제:

`theta += 8`로 바꾼 상태라고 가정한다.
실행 후 옳은 것을 모두 고른 것은?

- ㄱ. 이전보다 더 빠르게 회전한다.
- ㄴ. 중심은 그대로 유지된다.
- ㄷ. 배경색은 그대로다.
- ㄹ. 이미지가 자동으로 두 개로 늘어난다.

정답:

- `ㄱ, ㄴ, ㄷ`

해설:

- 각도 증가량만 커졌으므로 회전 속도만 빨라진다.
- 중심 보정 코드와 배경색 코드는 그대로라서 그 결과도 유지된다.

부분 정답 기준:

- `ㄱ, ㄴ` 또는 `ㄱ, ㄷ`이면 부분 정답
- `ㄱ, ㄴ, ㄷ`이면 정답

실제 실행 확인 결과:

- 이미지 수는 그대로고 회전만 빨라진다.

검수 체크:

- 속도 변화와 다른 요소 변화를 혼동하지 않게 설계되었는지 확인

### 4.3

난이도:

- 중

형식:

- 빈칸형

출제 의도:

- 회전 속도만 바꾸고 중심은 유지해야 한다는 점을 코드 조각으로 확인한다.

문제:

회전 속도만 바꾸고 중심은 그대로 유지하려면
아래 빈칸에 들어갈 코드 조각을 쓰시오.

```python
rotated_rect = rotated.get_rect(________)
```

정답:

- `center=(260, 160)`

해설:

- 회전 속도를 바꾸더라도 중심 보정 코드는 유지해야
  화면 가운데를 기준으로 회전하는 상태를 계속 볼 수 있다.

부분 정답 기준:

- 없음. `center=(260, 160)`만 정답 처리

실제 실행 확인 결과:

- 중심 보정 코드를 유지하면 속도가 달라도 회전 중심은 그대로 유지된다.

검수 체크:

- 속도와 중심 보정을 분리해서 묻고 있는지 확인

### 4.4

난이도:

- 중

형식:

- 복수정답 객관식형

출제 의도:

- 중심 보정과 각도 증가량 변경을 함께 반영한 결과를 종합 판단하게 한다.

문제:

`theta += 1`로 바꾸고
`rotated_rect = rotated.get_rect(center=(260, 160))`를 유지한 상태라고 가정한다.
실행 후 옳은 것을 모두 고른 것은?

- ㄱ. 이미지가 이전보다 느리게 회전한다.
- ㄴ. 이미지가 화면 가운데를 기준으로 돈다.
- ㄷ. 이미지가 다시 `(0, 0)` 기준으로 돌아간다.
- ㄹ. 배경색은 그대로다.

정답:

- `ㄱ, ㄴ, ㄹ`

해설:

- 각도 증가량이 줄어 느리게 회전한다.
- 중심 보정이 유지되므로 회전 중심도 그대로다.
- 위치 기준을 `(0, 0)`으로 되돌리는 코드는 없다.

부분 정답 기준:

- `ㄱ, ㄴ` 또는 `ㄴ, ㄹ`이면 부분 정답
- `ㄱ, ㄴ, ㄹ`이면 정답

실제 실행 확인 결과:

- 중심은 유지된 채 회전 속도만 느려진 결과를 확인할 수 있다.

검수 체크:

- 결과 예측과 수정 후 동작 설명이 모두 포함되는지 확인

---

## 총평

현재 `week02`는 모듈 02 핵심 축에 맞춰 아래 4개 대문항으로 재구성되었다.

- 이미지 출력 위치와 좌표 기준
- 회전 결과가 어색한 이유 찾기
- `get_rect(center=...)`와 중심 좌표 읽기
- 회전 속도와 결과 비교

문항 형식은 모두 아래 셋으로 제한했다.

- 코드 입력형
- 빈칸형
- 복수정답 객관식형

따라서 다음 장점이 있다.

- 회전 문제로 바로 들어가기 전 `blit` 위치와 좌표 기준을 먼저 고정할 수 있다.
- 회전 자체와 중심 보정을 분리해서 읽게 할 수 있다.
- `get_rect(center=...)`를 좌표 해석 문제로 바꿔 자동채점에 유리하게 만들 수 있다.
- 숫자 하나를 바꿨을 때 속도만 달라지는 결과를 코드 기준으로 설명하게 할 수 있다.

다음 단계 권장 순서:

1. 이 검수 원본 확정
2. 대문항별 기준 코드와 `problem01.py`~`problem04.py` 동기화 여부 점검
3. 필요 시 참고 PNG/GIF 추가 보강
4. 웹용 JSON 생성

---
