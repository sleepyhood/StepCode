---
id: "STRLv01002"
db_id: 2032
legacy_id: "STRLv01004"
title: "02. [스택 - 2] 괄호"
platform: "doingcoding"
level: "Low"
tags:
  - "STRLv01 Stack 1"
authors:
  - "root"
supported_languages:
  - "C"
  - "C++"
  - "Java"
  - "Python3"
time_limit: "1s"
memory_limit: "256MB"
accepted_user_count: 9
has_hint: "true"
archived_at: "2026-05-22"
---

# 02. [스택 - 2] 괄호

## 1. 문제 설명
괄호 문자열은 두개의 괄호 기호인  "("와 "')"만으로 이루어진 문자열이다. 그 중에 괄호의 모양이 바르게 구성된 문자열을 올바른 괄호 문자열이라고 부른다. 한쌍의 괄호 기호인 "()"은 기본 괄호 문자열이다. 만약 x가 올바른 괄호 문자열이라면, ()사이에 x를 넣은 (x)도 올바른 괄호 문자열이 된다. 올바른 괄호 문자열인 x와 y를 결합한 xy도 올바른 괄호 문자열이다. 예를 들어 "(())"와 "()()"은 올바른 괄호 문자열이지만 "))((", "(()", "(()())("은 올바른 괄호 문자열이 아니다.

---

예를 들어, "(())" 가 올바른 문자인지 판별하는 과정은 다음 다이어그램으로 표현할 수 있다:

![alt text](STRLv01002.svg)

---

입력으로 주어진 괄호 문자열이 올바른 괄호 문자열인지 아닌지 판단하는 프로그램을 작성해 주세요.

---

## 2. 입출력 설명

* **입력:**
"("와 ")"로만 이루어진 괄호 문자열이 주어집니다. (문자열의 길이는 2 이상 50 이하)

* **출력:**
올바른 괄호 문자열이면 YES, 아니라면 NO를 출력해 주세요.

---

## 3. 예시
### 예시 입력 1
```text
(())())
```

### 예시 출력 1
```text
NO
```

### 예시 입력 2
```text
(((()())()
```

### 예시 출력 2
```text
NO
```

### 예시 입력 3
```text
(()())((()))
```

### 예시 출력 3
```text
YES
```

### 예시 입력 4
```text
((()()(()))(((())))()
```

### 예시 출력 4
```text
NO
```

### 예시 입력 5
```text
()()()()(()()())()
```

### 예시 출력 5
```text
YES
```

---

## 4. 힌트

올바른 괄호 쌍을 찾는 문제는 스택의 **후입선출(LIFO)** 성질을 활용하는 대표적인 문제입니다.

* **여는 괄호 `(`** 를 만나면: "나중에 짝을 맞춰야 하니 기억해두자" ➡️ **스택에 Push**
* **닫는 괄호 `)`** 를 만나면: "가장 최근에 기억해둔 `(`와 짝을 지어 없애자" ➡️ **스택에서 Pop**


```mermaid
flowchart LR
    Step1["<b>1단계: '(' 읽음</b><br/>📥 Push<br/><br/>스택 상태:<br/><code>┌───┐<br/>│&nbsp;(&nbsp;│<br/>└───┘</code>"] --> Step2["<b>2단계: '(' 읽음</b><br/>📥 Push<br/><br/>스택 상태:<br/><code>┌───┐<br/>│&nbsp;(&nbsp;│<br/>├───┤<br/>│&nbsp;(&nbsp;│<br/>└───┘</code>"]
    Step2 --> Step3["<b>3단계: ')' 읽음</b><br/>📤 Pop<br/><br/>스택 상태:<br/><code>┌───┐<br/>│&nbsp;(&nbsp;│<br/>└───┘</code>"]
    Step3 --> Step4["<b>4단계: ')' 읽음</b><br/>📤 Pop<br/><br/>스택 상태:<br/><code>┌───┐<br/>│&nbsp;&nbsp;&nbsp;│<br/>└───┘</code><br/>(비어있음)"]
    
    Step4 --> Result{"<b>검사 종료 후</b><br/>스택이 비어있는가?"}
    Result -->|Yes| YES["<b>YES</b><br/>올바른 괄호"]
    Result -->|No| NO["<b>NO</b><br/>올바르지 않음"]

    %% 스타일 정의
    style Step1 fill:#f8fafc,stroke:#cbd5e1,stroke-width:2px
    style Step2 fill:#f8fafc,stroke:#cbd5e1,stroke-width:2px
    style Step3 fill:#f0f9ff,stroke:#0ea5e9,stroke-width:2px
    style Step4 fill:#f0fdf4,stroke:#22c55e,stroke-width:2px
    style Result fill:#fffbeb,stroke:#f59e0b,stroke-width:2px
    style YES fill:#d1fae5,stroke:#10b981,stroke-width:3px
    style NO fill:#fee2e2,stroke:#ef4444,stroke-width:3px
```