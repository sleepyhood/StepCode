## 목적

이 가이드는 모든 언어에 공통으로 적용되는 문제 제작 원칙과 템플릿을 정리한다.
언어별 문법은 예시에 의존하지 말고, 핵심 개념과 사고 과정을 평가하도록 설계한다.

---

## 0. 전체 큰 분류 (3축)

1. **읽기/이해형**: 코드를 읽고 의미 파악, 출력 예측
2. **판단/비교/디버깅형**: 옳고 그름 판단, 버그 찾기, 반례 찾기
3. **설계/작성형**: 변수/입력/조건/한 줄 코드 직접 설계

각 축의 템플릿을 섞어 반복 패턴을 줄인다.

---

## A. 읽기/이해 중심 템플릿

### A1. 코드 -> 자연어 설명 고르기 / 쓰기

**형식**

- 짧은 코드 한 덩어리 제시
- 질문: "이 코드는 어떤 일을 하는가?"
  - (1) 보기형(MCQ)
  - (2) 한 줄 서술형(단답형)

**예 템플릿(언어 중립)**

> [코드]
> 이 프로그램이 하는 일을 가장 잘 설명한 것은?

- A. 두 정수를 입력받아 합을 출력한다.
- B. 한 정수를 입력받아, 음수면 Minus를 출력한다.
- C. ...

**무지성 방지 포인트**

- 보기들을 유사하게 만들되, 조건/출력까지 읽어야 구별되게 구성
- 변수명/숫자만 보고 찍기 어렵게 설계

---

### A2. 자연어 -> 코드 골라오기

**형식**

- 자연어 요구사항 설명
- 서로 조금씩 다른 코드 3~4개 제시
- "설명에 딱 맞는 코드를 고르시오"

**무지성 방지 포인트**

- 오답도 거의 맞지만 미묘하게 다른 코드로 구성
  - 경계값 포함 여부
  - 조건식 순서
  - 입력 처리 위치

---

### A3. 실행 추적 / 출력 예측

**형식**

- 완성된 코드 + 입력 예시 하나
- "출력 결과를 줄바꿈까지 포함해 쓰시오" 또는 MCQ

**무지성 방지 포인트**

- 줄 순서, 개행 유무까지 구분되게 보기 설계
- 조건문 앞뒤에 출력문 배치해 흐름을 읽어야 풀 수 있게 구성

---

### A4. 조건식 판단표 (T/F 테이블)

**형식**

- 조건식 1개 제시
- 값 여러 개 제시
- 참/거짓 체크

**무지성 방지 포인트**

- 범위의 양 끝값과 바깥값을 섞어 범위 이해를 요구

---

## B. 판단 / 비교 / 디버깅 템플릿

### B1. 옳은/틀린 코드 판별

**형식**

- 코드 한 덩어리 + 의도 설명
- "이 코드는 설명대로 동작하는가?" (예/아니오)

**무지성 방지 포인트**

- 문법 오류가 아니라 논리 오류(경계값, 비교 연산, 조건식)를 중심으로

---

### B2. 버그 위치 찾기

**형식**

- 5~8줄 코드
- "의도: ~~. 하지만 한 줄 이상 잘못됨. 잘못된 줄 번호를 고르시오."

**무지성 방지 포인트**

- 겉보기로 애매한 줄을 섞어 실제 판단을 요구

---

### B3. 반례 찾기

**형식**

- 의도 설명 + 미묘하게 틀린 코드
- "이 코드가 틀린 동작을 하도록 만드는 입력 값을 하나 쓰시오"

**무지성 방지 포인트**

- 답이 여러 개가 되게 설계해 이해 기반 풀이 유도

---

### B4. 동등/비동등 코드 비교

**형식**

- 코드 A, B 제시
- "항상 같은 결과인가?" 또는 "어떤 입력에서 다른가?"

**무지성 방지 포인트**

- 표면만 비슷한 코드 대신 논리식 변형(드모르간 등) 중심으로 구성

---

## C. 설계 / 작성형 템플릿

### C1. 변수/입력 설계 표 채우기

**형식**

- 자연어 요구사항
- 표: 변수명 / 자료형 / 역할 / 예시 값

**무지성 방지 포인트**

- "최소 몇 개 변수로 가능한가" 같은 추가 질문으로 사고 유도
- 이후 코드 작성 문제와 연계

---

### C2. 조건식만 작성 (1줄)

**형식**

- if/while 틀 제공
- 괄호 안 조건식만 작성

**무지성 방지 포인트**

- 경계값 오류가 흔한 오답이 되도록 설계
- 동치 표현을 여러 개 허용할 수 있음

---

### C3. I/O 문장만 작성

**형식**

- 코드 일부 제공
- 입력/출력 한 줄만 작성

**무지성 방지 포인트**

- 변수 이름/타입이 달라져도 형식을 이해했는지 확인

---

### C4. 코드 블록 순서 맞추기

**형식**

- 코드 조각을 섞어서 제공
- "올바른 순서로 나열" 또는 번호 쓰기

**무지성 방지 포인트**

- 선언-입력-처리-출력 순서를 이해해야 풀 수 있게 구성

---

### C5. 부분 구현: 골격 코드 + TODO

**형식**

- 전체 코드 뼈대 제공
- 1~2줄만 TODO

**무지성 방지 포인트**

- TODO 위치가 매번 다르게 배치되도록 설계

---

## D. 변수 필요성 강화 규칙 (중요)

학생들이 "변수가 왜 필요한지"를 이해하도록 아래 유형을 반드시 섞는다.

1. **누적 변수(합/곱/개수)**
   - 예: 조건을 만족하는 값의 개수
2. **최대/최소 변수**
   - 첫 입력을 기준값으로 두는 이유를 묻는 문제
3. **이전 값 변수(prev)**
   - "이전 값보다 증가했는지" 같은 비교
4. **상태 변수(플래그)**
   - 조건을 한 번이라도 만족했는지 기록

**권장 규칙**

- 한 세트(연습장) 안에 위 4유형 중 최소 2개 포함
- 단순 출력/반복만 있는 문제는 연속 배치 금지

---

## E. 연계(시퀀스) 구성 예시

한 세트는 "읽기 -> 예상 -> 판별/비판 -> 작성" 흐름을 만든다.

예시 순서:

1. A1 (코드 -> 설명)
2. A3 (실행 추적)
3. B3 (반례 찾기)
4. C2 (조건식 작성)

---

## F. 무지성 방지 공통 팁

1. **숫자/범위 랜덤화**
   - 같은 템플릿이라도 매번 입력 범위를 바꾸기
2. **오답을 전형적 실수로 설계**
   - 경계값 누락, 조건식 방향 반전, 갱신 위치 오류
3. **연계형으로 묶기**
   - 앞 문제의 코드/조건이 뒤 문제에 다시 등장
4. **너무 긴 코드 금지**
   - 짧지만 반드시 생각해야 하는 문제를 여러 개

---

# G. 문항 형태 제약 (신규)

### G1. MCQ(객관식) 제한
- **찍기로 풀 수 있는 MCQ 금지**: 정의/용어만 묻는 문제는 지양
- **항상 코드/흐름을 읽어야** 답이 나오도록 보기 설계
- 가능하면 **MCQ → 단답/코드/그리드**로 전환
- 한 세트에서 MCQ 비중은 낮게(핵심 개념은 서술형/그리드로 검증)

### G2. 반복문/추적형은 Grid 권장
- 반복문 흐름(특히 누적/조건 포함)은 **표(grid)로 추적**하게 설계
- `answerUi.kind = "grid"`로 설정하고, 각 반복의 `i`, `sum` 등 핵심 변수를 기록
- “각 반복이 끝난 직후” 시점을 명시해 혼동 방지

---

## H. 역추적 중심 출제 원칙 (추가)

### H1. 외워서 못 푸는 조건
- **표면 암기 방지**: “봤던 거랑 비슷해서”로 풀 수 없게, 항상 새로운 맥락/값 사용
- **규칙 암기 방지**: “i는 반복이니까요” 같은 추상적 답변만으로는 정답이 나오지 않게 설계
- 정답은 반드시 **흐름/상태 변화**를 따라가야 산출되도록 구성

### H2. 역추적형 템플릿 추가
- **출력 → 입력/초기값 추론**
  - 출력(또는 최종 상태)만 주고, 입력/초기값/조건을 맞히기
- **중간 상태 복원**
  - 특정 반복 회차의 `i`, `sum`, `count`를 채우게 하기
- **분기 경로 추론**
  - if가 실행된 횟수, 또는 조건이 참인 반복 번호를 구하기

### H3. MCQ 최소화 운영
- MCQ는 **보조용**으로만 사용 (비중 최소화)
- 핵심 개념 검증은 **grid/단답/서술/코드**로 전환
- MCQ를 쓰는 경우에도 **오답이 전형적 실수**로 구성되도록 설계

---

## I. 문법 축약 지양 (추가)

- 학습자가 아직 배우지 않은 축약 문법(축약 대입, 삼항, 한 줄 축약 등)은 사용하지 않는다.
- 가능한 한 **배운 형태의 정식 문법**으로 작성한다.
- 한 줄로 가능한 코드라도 교육 목적상 **가독성과 단계적 이해**를 우선한다.

---

## J. 출력 형식 통일 (추가)

- 조건문(분기) 단원 **이전까지는** 출력 형식을 서식문자(`%d`, `%lf` 등)로 통일한다.

---

# (선택) 스키마 개요

템플릿을 기계적으로 다루고 싶다면, 아래와 같은 구조를 사용할 수 있다.
언어에 종속되지 않도록 code는 "문자열"로만 저장한다.

## 1. 연습장(Lab) 전체 JSON 구조

```jsonc
{
  "labId": "loop-basic-1",
  "title": "반복문 기초",
  "description": "반복, 조건, 누적 변수 연습",
  "concepts": [
    {
      "id": "range_len",
      "title": "range와 len",
      "summary": "0부터 len(lst)-1까지 반복합니다.",
      "example": "```python\nfor i in range(len(lst)):\n    print(lst[i])\n```",
      "algorithm": "```text\n1) i를 0부터 시작한다.\n2) i < len(lst)까지 반복한다.\n```",
      "media": [
        { "type": "image", "src": "assets/img/example.png", "alt": "설명 이미지", "caption": "간단 캡션" }
      ],
      "relatedProblems": ["q1", "q2"]
    }
  ],
  "questions": [
    {
      "id": "q1",
      "kind": "A1_CODE_TO_DESC",
      "meta": {
        "difficulty": "easy",
        "tags": ["loop", "trace"]
      },
      "params": {
        "stem": "이 프로그램이 하는 일을 가장 잘 설명한 것을 고르시오.",
        "code": "...",
        "options": ["...", "..."],
        "correctIndex": 1,
        "conceptRef": "range_len",
        "ioExample": { "input": "예시 입력", "output": "예시 출력" },
        "trace": [
          { "line": 1, "vars": { "i": 0 }, "note": "초기화" }
        ],
        "hints": ["힌트1", "힌트2"],
        "wrongFeedback": {
          "0": "전형적 오답 설명",
          "2": "경계값 실수"
        },
        "wrongFeedbackText": "단답/코드 오답 공통 메시지",
        "media": [
          { "type": "image", "src": "assets/img/q1.png", "alt": "문제 이미지", "caption": "보조 설명" }
        ]
      }
    }
  ]
}
```

## 2. 템플릿 키워드 예시

- A1_CODE_TO_DESC
- A2_DESC_TO_CODE
- A3_EXEC_TRACE
- A4_COND_TRUTH_TABLE
- B1_CORRECTNESS_JUDGE
- B2_BUG_LOCATE
- B3_FIND_COUNTEREXAMPLE
- B4_EQUIVALENCE
- C1_VAR_TABLE
- C2_COND_ONLY
- C3_IO_STATEMENT
- C4_BLOCK_ORDER
- C5_PARTIAL_IMPLEMENTATION

---

## (추가) 실전 스키마 가이드 (현재 프로젝트 기준)

### 1) concepts (개념 블록)
- `summary`, `example`, `algorithm`은 Markdown 문자열
- `media`는 이미지 배열(확장 가능)
- `relatedProblems`로 문제 연결

### 2) problems (문항)
- `conceptRef`: 연결 개념 id
- `ioExample`: 입/출력 예시 (단, **short(출력 예측)**에는 답 노출이 될 수 있어 지양)
- `trace`: 실행 흐름 단계(선택)
- `hints`: 단계형 힌트 배열 (일반 모드에서 순차 공개)
- `wrongFeedback`: MCQ 선택지별 피드백 (key=옵션 index)
- `wrongFeedbackText`: short/code 공통 오답 메시지
- `media`: 문제 이미지(선택)

### 3) 설계 원칙 보강
- **중복 방지**: 같은 목적의 문항은 하나만 유지하고, 다른 개념을 묻는 문항으로 교체
- **한 줄 수정형**: 버그 한 줄을 고치게 하는 문제로 디버깅 능력 강화

---

## (추가) 세트 스키마 체크리스트

### 필수 점검
- `concepts`와 각 문항의 `conceptRef`가 유효하게 연결되는가
- `hints`는 단계형으로 제공되는가 (필요 시)
- `wrongFeedback`(MCQ) / `wrongFeedbackText`(short, code)가 포함되는가
- `ioExample`이 **short(출력 예측)** 문제에 들어가 있지 않은가
- `media` 경로가 실제 파일과 일치하는가

### 권장 점검
- 동일 목적의 문항이 중복되지 않는가
- 한 줄 수정형 문제가 최소 1개 포함되는가
- Trace는 개념 이해가 필요한 문제에만 제한적으로 사용하는가
- MCQ는 찍기로 풀 수 없도록 설계되어 있는가
- 반복문/추적형 문제는 grid 사용이 반영되었는가
- 역추적형(출력→입력/중간상태/분기 추론)이 포함되었는가
- 외워서 풀 수 있는 유형이 과도하지 않은가 (값/맥락 변형 여부)
